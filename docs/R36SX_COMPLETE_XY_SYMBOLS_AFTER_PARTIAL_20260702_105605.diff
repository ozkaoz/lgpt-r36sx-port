--- /home/dafunknoise/r36sx-lgpt-port/backups/source_backups_20260702_105605/GUIEvent.cpp.before_xy_complete	2026-05-17 03:53:03.000000000 -0500
+++ /home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V100_DEV_NEXT/sources/UIFramework/BasicDatas/GUIEvent.cpp	2026-07-02 10:56:05.562080600 -0500
@@ -75,6 +75,12 @@
 case EPBT_SELECT: 
 	value="EPBT_SELECT" ;
 	break;
+case EPBT_X:
+	value="EPBT_X" ;
+	break ;
+case EPBT_Y:
+	value="EPBT_Y" ;
+	break ;
 	}
 }
 
--- /home/dafunknoise/r36sx-lgpt-port/backups/source_backups_20260702_105605/View.h.before_xy_complete	2026-05-17 03:53:03.000000000 -0500
+++ /home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V100_DEV_NEXT/sources/Application/Views/BaseClasses/View.h	2026-07-02 10:56:05.563584100 -0500
@@ -24,6 +24,10 @@
     EPBM_L = 16,
     EPBM_B = 32,
     EPBM_A = 64,
+    // TREEFROG_INPUT_XY_EVENT_MASKS
+    // Reservados para separar X/Y de A/B en pasos posteriores.
+    EPBM_X = 1024,
+    EPBM_Y = 2048,
     EPBM_R = 128,
     EPBM_START = 256,
     EPBM_SELECT = 512,
--- /home/dafunknoise/r36sx-lgpt-port/backups/source_backups_20260702_105605/TreeFrogEventManager.cpp.before_xy_complete	2026-07-02 00:39:03.000000000 -0500
+++ /home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V100_DEV_NEXT/sources/Adapters/TREEFROG/GUI/TreeFrogEventManager.cpp	2026-07-02 10:56:05.563584100 -0500
@@ -38,6 +38,8 @@
         case EPBT_DOWN: return "DOWN";
         case EPBT_A: return "A";
         case EPBT_B: return "B";
+        case EPBT_X: return "X";
+        case EPBT_Y: return "Y";
         case EPBT_L: return "L";
         case EPBT_R: return "R";
         case EPBT_START: return "START";
@@ -84,6 +86,8 @@
     if (!strcmp(name, "down")) return EPBT_DOWN;
     if (!strcmp(name, "a")) return EPBT_A;
     if (!strcmp(name, "b")) return EPBT_B;
+    if (!strcmp(name, "x")) return EPBT_X;
+    if (!strcmp(name, "y")) return EPBT_Y;
     if (!strcmp(name, "l")) return EPBT_L;
     if (!strcmp(name, "r")) return EPBT_R;
     if (!strcmp(name, "start")) return EPBT_START;
--- /home/dafunknoise/r36sx-lgpt-port/backups/source_backups_20260702_105605/TreeFrogLibretro.cpp.before_xy_complete	2026-07-02 00:51:48.000000000 -0500
+++ /home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V100_DEV_NEXT/sources/Adapters/TREEFROG/Main/TreeFrogLibretro.cpp	2026-07-02 10:56:05.564588900 -0500
@@ -187,6 +187,8 @@
     { EPBT_DOWN,   "LGPT_DOWN",   false, false },
     { EPBT_A,      "LGPT_A",      false, false },
     { EPBT_B,      "LGPT_B",      false, false },
+    { EPBT_X,      "LGPT_X",      false, false },
+    { EPBT_Y,      "LGPT_Y",      false, false },
     { EPBT_L,      "LGPT_L",      false, false },
     { EPBT_R,      "LGPT_R",      false, false },
     { EPBT_START,  "LGPT_START",  false, false },
@@ -250,7 +252,9 @@
     /* R36SX-friendly safe duplicates.  On this hardware the right-stick lines
      * are digital face-button aliases on some revisions, so duplicates are
      * aggregated before dispatch and cannot create double down/up events. */
+    // TREEFROG_INPUT_XY_DUPLICATE_COMPAT: X sigue duplicando A hasta activar X dedicado.
     if (phys & PHY_BIT(PHY_X))     output_set(EPBT_A);
+    // TREEFROG_INPUT_XY_DUPLICATE_COMPAT: Y sigue duplicando B hasta activar Y dedicado.
     if (phys & PHY_BIT(PHY_Y))     output_set(EPBT_B);
 #else
     /* Diagnostic mode: face buttons are injected as rising-edge taps after
@@ -385,8 +389,10 @@
 #if TREEFROG_FACE_TAP_MODE == 1
     uint32_t rising = phys & ~last_phys_for_taps;
     if (rising & PHY_BIT(PHY_A)) push_face_tap(EPBT_A);
+    // TREEFROG_INPUT_XY_DUPLICATE_COMPAT: face-tap X conserva A.
     if (rising & PHY_BIT(PHY_X)) push_face_tap(EPBT_A);
     if (rising & PHY_BIT(PHY_B)) push_face_tap(EPBT_B);
+    // TREEFROG_INPUT_XY_DUPLICATE_COMPAT: face-tap Y conserva B.
     if (rising & PHY_BIT(PHY_Y)) push_face_tap(EPBT_B);
 #else
     (void)phys;
