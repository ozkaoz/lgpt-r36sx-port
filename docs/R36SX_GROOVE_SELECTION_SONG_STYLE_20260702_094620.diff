--- /home/dafunknoise/r36sx-lgpt-port/backups/source_backups_20260702_094620/GrooveView.cpp.before_groove_selection_song_style	2026-05-17 03:53:03.000000000 -0500
+++ /home/dafunknoise/r36sx-lgpt-port/dev_sources/PORT_LGPT_V2_DEV_UI_REFACTOR/sources/Application/Views/GrooveView.cpp	2026-07-02 09:46:20.313854100 -0500
@@ -170,8 +170,19 @@
 		} else {
 			strcpy(buffer,"--") ;
 		} ;
-		props.invert_=(j==position_) ; 
+		// TREEFROG_UI_GROOVE_SELECTION_SONG_STYLE
+		// Usar la misma semántica visual validada en Song:
+		// CD_HILITE2 + invert_ para la celda activa.
+		if (j==position_) {
+		    SetColor(CD_HILITE2);
+		    props.invert_ = true;
+		} else {
+		    SetColor(CD_NORMAL);
+		    props.invert_ = false;
+		}
 		DrawString(pos._x,pos._y,buffer,props) ;
+		props.invert_ = false;
+		SetColor(CD_NORMAL);
 		pos._y++ ;
 	}
 
