--- /home/dafunknoise/r36sx-lgpt-port/backups/source_backups_20260702_111506/SongView.h.before_ab_clear_yx_cut	2026-05-17 03:53:03.000000000 -0500
+++ /home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V101_DEV_NEXT/sources/Application/Views/SongView.h	2026-07-02 11:15:06.865056700 -0500
@@ -27,6 +27,7 @@
     void updateCursor(int dx, int dy);
     void setChain(unsigned char);
     void cutPosition();
+    void clearPosition();
     void clonePosition();
     void deepClonePosition();
     void pasteLast();
--- /home/dafunknoise/r36sx-lgpt-port/backups/source_backups_20260702_111506/SongView.cpp.before_ab_clear_yx_cut	2026-07-02 08:37:40.726638600 -0500
+++ /home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V101_DEV_NEXT/sources/Application/Views/SongView.cpp	2026-07-02 11:15:06.866061600 -0500
@@ -115,6 +115,20 @@
 
     // cut selection
     cutSelection();
+}
+
+// TREEFROG_INPUT_SONG_AB_CLEAR_YX_CUT
+// Clear de una celda Song sin compactar filas.
+// Deja la posición actual como -- usando el mismo sentinel 0xFF que ya usa SongView.
+void SongView::clearPosition() {
+    int col = (int)viewData_->songX_;
+    int row = (int)viewData_->songY_ + (int)viewData_->songOffset_;
+
+    if (col < 0 || col >= SONG_CHANNEL_COUNT) return;
+    if (row < 0 || row >= SONG_ROW_COUNT) return;
+
+    unsigned char *cell = viewData_->song_->data_ + row * SONG_CHANNEL_COUNT + col;
+    *cell = 0xFF;
 };
 
 /******************************************************
@@ -681,7 +695,9 @@
             isDirty_ = true;
         }
         if ((mask & EPBM_A) && (!(mask & EPBM_R)))
-            cutPosition();
+            // TREEFROG_INPUT_SONG_AB_CLEAR_YX_CUT
+            // A+B ahora limpia la celda actual sin compactar filas.
+            clearPosition();
         if (mask & EPBM_L) {
 
             viewMode_ = VM_CLONE;
