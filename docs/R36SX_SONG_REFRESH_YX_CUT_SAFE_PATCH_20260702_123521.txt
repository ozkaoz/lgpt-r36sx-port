R36SX Song refresh + YX cut safe patch
Generated: 20260702_123521
DEV=/home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V101_DEV_NEXT
SONG=/home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V101_DEV_NEXT/sources/Application/Views/SongView.cpp
BACKUP=/home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V101_DEV_NEXT/backups/R36SX_SONG_REFRESH_YX_CUT_SAFE_20260702_123521/SongView.cpp.20260702_123521.bak

=== Y+X V2 context ===
680-        selection active
681- ******************************************************/
682-
683-void SongView::processNormalButtonMask(unsigned int mask) {
684-
685-
686:    // TREEFROG_INPUT_SONG_YX_CUT_NORMAL_MODE_V2
687-    // R36SX TreeFrog:
688-    // Y+X ejecuta el cut/compact histórico de Song.
689-    // No exigimos máscara exacta porque el runtime puede mantener otros bits latcheados.
690-    const unsigned int treefrogSongYXMask = EPBM_Y | EPBM_X;
691-    if ((mask & treefrogSongYXMask) == treefrogSongYXMask) {
692-        cutPosition();
693-        isDirty_ = true;
694-        SetDirty(true);
695-        return;
696-    }
697-
698-    // B Modifier
699-
700-    if (mask & EPBM_B) {
701-
702-        if (mask & EPBM_DOWN)
703-            updateSongOffset(SongView::jumpLength_);
704-        if (mask & EPBM_UP)

=== Existing A+B clear context ===
707-            Player *player = Player::GetInstance();
708-            switch (player->GetSequencerMode()) {
709-            case SM_SONG:
710-                player->SetSequencerMode(SM_LIVE);
711-                break;
712-            case SM_LIVE:
713-                player->SetSequencerMode(SM_SONG);
714-                break;
715-            }
716-            isDirty_ = true;
717-        }
718-        if ((mask & EPBM_A) && (!(mask & EPBM_R)))
719-            // TREEFROG_INPUT_SONG_AB_CLEAR_YX_CUT
720-            // A+B ahora limpia la celda actual sin compactar filas.
721:            clearPosition();
722-        if (mask & EPBM_L) {
723-
724-            viewMode_ = VM_CLONE;
725-        };
726-        if (mask & EPBM_R) {
727-            toggleMute();
728-        };
729-        if (mask & EPBM_START) {
730-            startImmediate();
731-        }
732-    } else {
733-
734-        // A modifier
735-

=== clearPosition dirty context ===
125-// Clear de una celda Song sin compactar filas.
126-// Deja la posición actual como -- usando el mismo sentinel 0xFF que ya usa SongView.
127-void SongView::clearPosition() {
128-    int col = (int)viewData_->songX_;
129-    int row = (int)viewData_->songY_ + (int)viewData_->songOffset_;
130-
131-    if (col < 0 || col >= SONG_CHANNEL_COUNT) return;
132-    if (row < 0 || row >= SONG_ROW_COUNT) return;
133-
134-    unsigned char *cell = viewData_->song_->data_ + row * SONG_CHANNEL_COUNT + col;
135-    *cell = 0xFF;
136-
137:    // TREEFROG_SONG_CLEAR_FORCE_DIRTY_V2
138-    isDirty_ = true;
139-    SetDirty(true);
140-};
141-
142-/******************************************************
143- pastePosition:
144-        set current position to last chain value if
145-        current step is empty
146- ******************************************************/
147-
148-void SongView::pasteLast() {
149-

=== cutPosition dirty context ===
107-    // prepare selection data
108-    clipboard_.x_ = viewData_->songX_;
109-    clipboard_.y_ = viewData_->songY_;
110-    clipboard_.offset_ = viewData_->songOffset_;
111-
112-    saveX_ = viewData_->songX_;
113-    saveY_ = viewData_->songY_;
114-    saveOffset_ = viewData_->songOffset_;
115-
116-    // cut selection
117-    cutSelection();
118-
119:    // TREEFROG_SONG_CUT_FORCE_DIRTY_V2
120-    isDirty_ = true;
121-    SetDirty(true);
122-}
123-
124-// TREEFROG_INPUT_SONG_AB_CLEAR_YX_CUT
125-// Clear de una celda Song sin compactar filas.
126-// Deja la posición actual como -- usando el mismo sentinel 0xFF que ya usa SongView.
127-void SongView::clearPosition() {
128-    int col = (int)viewData_->songX_;
129-    int row = (int)viewData_->songY_ + (int)viewData_->songOffset_;
130-
131-    if (col < 0 || col >= SONG_CHANNEL_COUNT) return;

=== Diff ===
--- /home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V101_DEV_NEXT/backups/R36SX_SONG_REFRESH_YX_CUT_SAFE_20260702_123521/SongView.cpp.20260702_123521.bak	2026-07-02 12:23:20.498818000 -0500
+++ /home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V101_DEV_NEXT/sources/Application/Views/SongView.cpp	2026-07-02 12:35:21.375239400 -0500
@@ -115,6 +115,10 @@
 
     // cut selection
     cutSelection();
+
+    // TREEFROG_SONG_CUT_FORCE_DIRTY_V2
+    isDirty_ = true;
+    SetDirty(true);
 }
 
 // TREEFROG_INPUT_SONG_AB_CLEAR_YX_CUT
@@ -129,6 +133,10 @@
 
     unsigned char *cell = viewData_->song_->data_ + row * SONG_CHANNEL_COUNT + col;
     *cell = 0xFF;
+
+    // TREEFROG_SONG_CLEAR_FORCE_DIRTY_V2
+    isDirty_ = true;
+    SetDirty(true);
 };
 
 /******************************************************
@@ -674,14 +682,17 @@
 
 void SongView::processNormalButtonMask(unsigned int mask) {
 
-    // TREEFROG_INPUT_SONG_YX_CUT_NORMAL_MODE
-    // R36SX TreeFrog: physical Y+X keeps the historical Song cut/compact behavior.
-    // Exact combo only. A+B remains clearPosition().
+
+    // TREEFROG_INPUT_SONG_YX_CUT_NORMAL_MODE_V2
+    // R36SX TreeFrog:
+    // Y+X ejecuta el cut/compact histórico de Song.
+    // No exigimos máscara exacta porque el runtime puede mantener otros bits latcheados.
     const unsigned int treefrogSongYXMask = EPBM_Y | EPBM_X;
-    if ((mask & treefrogSongYXMask) == treefrogSongYXMask &&
-        (mask & ~treefrogSongYXMask) == 0) {
+    if ((mask & treefrogSongYXMask) == treefrogSongYXMask) {
         cutPosition();
-        mask = 0;
+        isDirty_ = true;
+        SetDirty(true);
+        return;
     }
 
     // B Modifier
