--- /home/dafunknoise/r36sx-lgpt-port/backups/source_backups_20260702_110002/TreeFrogLibretro.cpp.before_y_dedicated	2026-07-02 10:56:05.564588900 -0500
+++ /home/dafunknoise/r36sx-lgpt-port/dev_sources/LGPT_PORT_V100_DEV_NEXT/sources/Adapters/TREEFROG/Main/TreeFrogLibretro.cpp	2026-07-02 11:00:02.511597300 -0500
@@ -255,7 +255,8 @@
     // TREEFROG_INPUT_XY_DUPLICATE_COMPAT: X sigue duplicando A hasta activar X dedicado.
     if (phys & PHY_BIT(PHY_X))     output_set(EPBT_A);
     // TREEFROG_INPUT_XY_DUPLICATE_COMPAT: Y sigue duplicando B hasta activar Y dedicado.
-    if (phys & PHY_BIT(PHY_Y))     output_set(EPBT_B);
+    // TREEFROG_INPUT_Y_DEDICATED: Y deja de duplicar B y queda disponible para funciones nuevas.
+    if (phys & PHY_BIT(PHY_Y))     output_set(EPBT_Y);
 #else
     /* Diagnostic mode: face buttons are injected as rising-edge taps after
      * latched direction/shoulder state has been updated.  This avoids leaving
@@ -393,7 +394,8 @@
     if (rising & PHY_BIT(PHY_X)) push_face_tap(EPBT_A);
     if (rising & PHY_BIT(PHY_B)) push_face_tap(EPBT_B);
     // TREEFROG_INPUT_XY_DUPLICATE_COMPAT: face-tap Y conserva B.
-    if (rising & PHY_BIT(PHY_Y)) push_face_tap(EPBT_B);
+    // TREEFROG_INPUT_Y_DEDICATED: face-tap Y usa EPBT_Y.
+    if (rising & PHY_BIT(PHY_Y)) push_face_tap(EPBT_Y);
 #else
     (void)phys;
 #endif
