--- /tmp/au10z_srcfull/LGPT_PORT_U2_50_FINAL_MIXER_MASTER_SOURCE/sources/Adapters/TREEFROG/Audio/TreeFrogAudioDriver.cpp	2026-07-10 00:31:57.000000000 +0000
+++ /mnt/data/U2_38AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/source_full/LGPT_PORT_U2_50_AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/sources/Adapters/TREEFROG/Audio/TreeFrogAudioDriver.cpp	2026-07-10 01:20:33.777908813 +0000
@@ -247,8 +247,12 @@
 #endif
 
     if (!isPlaying_ || !g_treefrog_playback_armed_v14) {
-        /* V14_RENDER_NOT_ARMED_SILENCE: do not call OnNewBufferNeeded() while LGPT is idle/menu-only. */
+        /* AU11_USB_MONITOR_IDLE_RENDER: keep the audio callback alive for
+         * USB-C RECORD prelisten even when the sequencer/project transport is
+         * stopped. This makes the console behave like a sampler input monitor:
+         * silence is rendered first, then USB input monitor is mixed locally. */
         memset(dst, 0, frames * 2 * sizeof(int16_t));
+        TreeFrogUac2Bridge_MixUsbCaptureMonitorStereo44100(dst, frames);
         rendering_ = false;
         return;
     }
--- /tmp/au10z_srcfull/LGPT_PORT_U2_50_FINAL_MIXER_MASTER_SOURCE/sources/Adapters/TREEFROG/Audio/TreeFrogUac2Bridge.cpp	2026-07-10 01:05:01.000000000 +0000
+++ /mnt/data/U2_38AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/source_full/LGPT_PORT_U2_50_AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/sources/Adapters/TREEFROG/Audio/TreeFrogUac2Bridge.cpp	2026-07-10 01:20:57.825277667 +0000
@@ -15,9 +15,16 @@
 #endif
 
 #if TREEFROG_UAC2_BRIDGE
+
+extern "C" const char *TreeFrogAu11UsbSamplerCleanBuildMarker(void) {
+    return "U2_38AU11_USB_SAMPLER_CLEAN FULL_SOURCE LOCAL_ONLY USB_OUT_AUTO_MUTE SCPI_R USB_C_RECORD AU11_IMPORT_MODAL_STOP_FIRST_A AU11_USB_MONITOR_IDLE_RENDER";
+}
 enum {
     AU10Y_LOCAL_ONLY = 0,
     AU10Y_USB_OUT_AUTO_MUTE = 1,
+    /* AU11: FULL_DUPLEX is no longer exposed as a third user mode.
+       The USB gadget remains physically duplex at all times; input is handled
+       exclusively by USB-C RECORD / SCPI-R to match sampler workflow. */
     AU10Y_FULL_DUPLEX = 2,
     AU10Y_USB_IN_CAPTURE = 99
 };
@@ -90,7 +97,7 @@
 static const char *mode_name(int mode) {
     switch (mode) {
         case AU10Y_LOCAL_ONLY: return "LOCAL_ONLY";
-        case AU10Y_FULL_DUPLEX: return "FULL_DUPLEX";
+        case AU10Y_FULL_DUPLEX: return "USB_OUT_AUTO_MUTE";
         case AU10Y_USB_IN_CAPTURE: return "USB_OUT_AUTO_MUTE";
         case AU10Y_USB_OUT_AUTO_MUTE:
         default: return "USB_OUT_AUTO_MUTE";
@@ -102,7 +109,7 @@
     switch (mode) {
         case AU10Y_LOCAL_ONLY: return "audio_driver_local_only";
         case AU10Y_USB_OUT_AUTO_MUTE: return "audio_driver_usb_out_automute";
-        case AU10Y_FULL_DUPLEX: return "audio_driver_full_duplex_usb_priority";
+        case AU10Y_FULL_DUPLEX: return "audio_driver_usb_out_automute";
         case AU10Y_USB_IN_CAPTURE: return "audio_driver_usb_out_automute";
         default: return "audio_driver_usb_out_automute";
     }
@@ -134,26 +141,25 @@
 static const char *mode_desc(int mode) {
     switch (mode) {
         case AU10Y_LOCAL_ONLY: return "Console speaker only";
-        case AU10Y_USB_OUT_AUTO_MUTE: return "USB OUT only; console muted";
-        case AU10Y_FULL_DUPLEX: return "FULL_DUPLEX: USB Audio playback+recording";
-        case AU10Y_USB_IN_CAPTURE: return "Removed; use FULL_DUPLEX";
+        case AU10Y_USB_OUT_AUTO_MUTE: return "USB audio out; USB-C REC handles input";
+        case AU10Y_FULL_DUPLEX: return "Hidden: duplex gadget, use USB-C REC";
+        case AU10Y_USB_IN_CAPTURE: return "Removed; use USB-C REC";
         default: return "Unknown";
     }
 }
 
 static int selectable_mode(int mode) {
-    /* AU10Y: keep the stable two output modes and expose only FULL_DUPLEX
-       as the USB input experiment. USB_IN_CAPTURE is intentionally removed
-       from the selectable menu because it duplicated the capture path and
-       destabilized mode switching in AU10N/AU10Q. */
-    return mode == AU10Y_LOCAL_ONLY || mode == AU10Y_USB_OUT_AUTO_MUTE || mode == AU10Y_FULL_DUPLEX;
+    /* AU11: expose only the two modes that have distinct sampler behavior.
+       The descriptor remains duplex internally, but USB input is controlled
+       by USB-C RECORD / SCPI-R, not by a duplicate Audio Driver item. */
+    return mode == AU10Y_LOCAL_ONLY || mode == AU10Y_USB_OUT_AUTO_MUTE;
 }
 
 static int mode_from_text(const char *s) {
     if (!s) return AU10Y_USB_OUT_AUTO_MUTE;
     if (strstr(s, "LOCAL_ONLY")) return AU10Y_LOCAL_ONLY;
     if (strstr(s, "USB_OUT_PLUS_LOCAL")) return AU10Y_USB_OUT_AUTO_MUTE;
-    if (strstr(s, "FULL_DUPLEX")) return AU10Y_FULL_DUPLEX;
+    if (strstr(s, "FULL_DUPLEX")) return AU10Y_USB_OUT_AUTO_MUTE; /* AU11 compatibility alias */
     if (strstr(s, "USB_IN_CAPTURE")) return AU10Y_USB_OUT_AUTO_MUTE;
     if (strstr(s, "USB_OUT_AUTO_MUTE")) return AU10Y_USB_OUT_AUTO_MUTE;
     return AU10Y_USB_OUT_AUTO_MUTE;
@@ -164,7 +170,7 @@
     if (!f) { f = fopen("/tmp/r36sx_uac2_bridge_lgpt.log", "a"); }
     if (!f) return;
     fprintf(f,
-            "AU10Y %s errno=%d (%s) submit=%lu mode=%s raw=%d out=%d mute=%d mixer=%d master=%d phase=%.6f fifo=%d marker=%d capture=%s\n",
+            "AU11 %s errno=%d (%s) submit=%lu mode=%s raw=%d out=%d mute=%d mixer=%d master=%d phase=%.6f fifo=%d marker=%d capture=%s\n",
             msg ? msg : "log", errno, strerror(errno), g_submit_count,
             TreeFrogUac2Bridge_GetDriverModeName(), g_usb_raw, g_usb_out_allowed,
             g_was_muted, g_mixer_volume_percent, g_project_master_volume_percent,
@@ -196,7 +202,7 @@
     const char *m = mode_name(mode);
     pid_t pid = fork();
     if (pid == 0) {
-        execl("/bin/sh", "sh", "/mnt/sdcard/lgpt/otg/bin/otg_38au10y_apply_profile_once.sh", m, (char *)0);
+        execl("/bin/sh", "sh", "/mnt/sdcard/lgpt/otg/bin/otg_38au11_apply_profile_once.sh", m, (char *)0);
         _exit(127);
     }
 }
@@ -286,6 +292,8 @@
     }
 }
 
+static int capture_recording_active(void); /* AU11 forward */
+
 static void refresh_usb_state(void) {
     if (g_submit_count != 0 && (g_submit_count - g_last_usb_check_submit) < 2) return;
     g_last_usb_check_submit = g_submit_count;
@@ -307,7 +315,7 @@
     /* AU10Y: USB_OUT_AUTO_MUTE must mute the local console as soon as the
        USB gadget is configured. AU10B depended on the playback-active marker,
        so Windows could be configured while local audio was still audible. */
-    return (g_driver_mode == AU10Y_USB_OUT_AUTO_MUTE || g_driver_mode == AU10Y_FULL_DUPLEX) && !exists_file(kNoMute) && g_usb_raw;
+    return (g_driver_mode == AU10Y_USB_OUT_AUTO_MUTE) && !g_usb_monitor_enabled && !capture_recording_active() && !exists_file(kNoMute) && g_usb_raw;
 }
 
 static void ensure_setup_started(void) {
@@ -319,14 +327,14 @@
 #if defined(PLATFORM_TREEFROG) || 1
     pid_t pid = fork();
     if (pid == 0) {
-        int fd = open("/mnt/sdcard/lgpt/otg/logs/u2_38au10y_setup_from_lgpt.log", O_WRONLY | O_CREAT | O_TRUNC, 0666);
-        if (fd < 0) fd = open("/tmp/u2_38au10y_setup_from_lgpt.log", O_WRONLY | O_CREAT | O_TRUNC, 0666);
+        int fd = open("/mnt/sdcard/lgpt/otg/logs/u2_38au11_setup_from_lgpt.log", O_WRONLY | O_CREAT | O_TRUNC, 0666);
+        if (fd < 0) fd = open("/tmp/u2_38au11_setup_from_lgpt.log", O_WRONLY | O_CREAT | O_TRUNC, 0666);
         if (fd >= 0) { dup2(fd, 1); dup2(fd, 2); if (fd > 2) close(fd); }
-        execl("/bin/sh", "sh", "/mnt/sdcard/lgpt/otg/bin/otg_38au10y_lgpt_sync_setup_once.sh", (char *)0);
+        execl("/bin/sh", "sh", "/mnt/sdcard/lgpt/otg/bin/otg_38au11_lgpt_sync_setup_once.sh", (char *)0);
         _exit(127);
     }
 #else
-    system("sh /mnt/sdcard/lgpt/otg/bin/otg_38au10y_lgpt_sync_setup_once.sh >/mnt/sdcard/lgpt/otg/logs/u2_38au10y_setup_from_lgpt.log 2>&1 &");
+    system("sh /mnt/sdcard/lgpt/otg/bin/otg_38au11_lgpt_sync_setup_once.sh >/mnt/sdcard/lgpt/otg/logs/u2_38au11_setup_from_lgpt.log 2>&1 &");
 #endif
 }
 
@@ -544,8 +552,8 @@
     refresh_usb_state();
     if ((g_submit_count % 12) == 0) refresh_passive_physical_volume_file();
     if ((g_submit_count % 60) == 0) refresh_capture_status();
-    if (g_driver_mode == AU10Y_LOCAL_ONLY || g_driver_mode == AU10Y_USB_IN_CAPTURE || (g_driver_mode == AU10Y_FULL_DUPLEX && capture_recording_active()) || !g_usb_out_allowed) {
-        close_fifo_if_open("fifo closed local-only-capture-recording-or-usb-inactive");
+    if (g_driver_mode == AU10Y_LOCAL_ONLY || g_driver_mode == AU10Y_USB_IN_CAPTURE || capture_recording_active() || g_usb_monitor_enabled || !g_usb_out_allowed) {
+        close_fifo_if_open("fifo closed local-only-usb-record-monitor-or-usb-inactive");
         return;
     }
     ensure_fifo_open_nonblocking();
@@ -673,14 +681,14 @@
 const char *TreeFrogUac2Bridge_CycleDriverMode(void) {
 #if TREEFROG_UAC2_BRIDGE
     int next = g_driver_mode + 1;
-    if (next > AU10Y_FULL_DUPLEX) next = AU10Y_LOCAL_ONLY;
+    if (next > AU10Y_USB_OUT_AUTO_MUTE) next = AU10Y_LOCAL_ONLY;
     return TreeFrogUac2Bridge_SetDriverMode(next);
 #else
     return "DISABLED";
 #endif
 }
 
-int TreeFrogUac2Bridge_GetDriverModeCount(void) { return 3; }
+int TreeFrogUac2Bridge_GetDriverModeCount(void) { return 2; }
 
 const char *TreeFrogUac2Bridge_GetDriverModeNameByIndex(int mode) {
 #if TREEFROG_UAC2_BRIDGE
@@ -857,7 +865,10 @@
         close(cfd);
         au10z_mirror_runtime_file("usb_capture_cmd", cmd);
     }
-    if (!g_usb_monitor_enabled) close_monitor_fifo(); else ensure_monitor_fifo_open();
+    if (!g_usb_monitor_enabled) close_monitor_fifo();
+    /* AU11: do not synchronously open the monitor FIFO from the UI thread.
+       The render callback opens it lazily, which removes the frozen first
+       seconds when entering USB-C RECORD. */
     log_msg(g_usb_monitor_enabled ? "capture monitor requested on" : "capture monitor requested off");
     return 1;
 #else
--- /tmp/au10z_srcfull/LGPT_PORT_U2_50_FINAL_MIXER_MASTER_SOURCE/sources/Application/Views/InstrumentView.cpp	2026-07-10 01:03:35.000000000 +0000
+++ /mnt/data/U2_38AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/source_full/LGPT_PORT_U2_50_AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/sources/Application/Views/InstrumentView.cpp	2026-07-10 01:20:33.779010344 +0000
@@ -39,7 +39,7 @@
     return instrument->GetType() ;
 } ;
 
-// AU10Z_SAFE_SAMPLE_MODAL_TRANSPORT_STOP
+// AU11_SAFE_SAMPLE_MODAL_TRANSPORT_STOP
 // Any sample/chop/record modal owns sample memory and/or audio streaming state.
 // Stop the project transport and preview streamer before opening those modals
 // to avoid reentrant sample-pool/audio teardown while LGPT is rendering.
@@ -50,7 +50,7 @@
     if (player->IsRunning()) { player->Stop(); touched = true; }
     if (player->IsStreaming()) { player->StopStreaming(); touched = true; }
     if (touched) {
-        Trace::Log("AU10Z", "SAFE_SAMPLE_MODAL_TRANSPORT_STOP %s", reason ? reason : "modal");
+        Trace::Log("AU11", "SAFE_SAMPLE_MODAL_TRANSPORT_STOP %s", reason ? reason : "modal");
         (void)reason;
         TimeService::GetInstance()->Sleep(120);
     }
@@ -324,7 +324,7 @@
     }
 
 
-    // AU10Z_SCPI_R_USB_RECORD_SHORTCUT
+    // AU11_SCPI_R_USB_RECORD_SHORTCUT
     // Hidden Instrument route: L1+LEFT opens USB-C RECORD directly from Instrument.
     // It still uses the stable SampleChopperModal implementation, but starts it
     // with the record branch visible, avoiding the Instrument -> Chopper -> USB path.
@@ -458,6 +458,9 @@
 
         if (mask == EPBM_A) {
             FourCC varID = ((UIIntVarField *)GetFocus())->GetVariableID();
+            if (varID == SIP_SAMPLE) {
+                au10zStopTransportBeforeSampleModal("AU11_IMPORT_MODAL_STOP_FIRST_A");
+            }
             if ((varID == SIP_TABLE) || (varID == MIP_TABLE) ||
                 (varID == SIP_SAMPLE) || (varID == SIP_PRINTFX)) {
                 viewMode_ = VM_NEW;
--- /tmp/au10z_srcfull/LGPT_PORT_U2_50_FINAL_MIXER_MASTER_SOURCE/sources/Application/Views/ModalDialogs/ImportSampleDialog.cpp	2026-07-10 00:31:57.000000000 +0000
+++ /mnt/data/U2_38AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/source_full/LGPT_PORT_U2_50_AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/sources/Application/Views/ModalDialogs/ImportSampleDialog.cpp	2026-07-10 01:20:33.779398941 +0000
@@ -4,6 +4,7 @@
 #include "Application/Instruments/SampleInstrument.h"
 #include "Application/Instruments/WavFile.h"
 #include "Services/Time/TimeService.h"
+#include "Application/Player/Player.h"
 #include "Application/Views/ModalDialogs/SampleManagerDialog.h"
 // TREEFROG_U2_34_SAMPLE_MANAGER_PURGE
 // TREEFROG_U2_35_SAMPLE_MANAGER_IMPORT_FORCE_DELETE
@@ -167,6 +168,14 @@
 } ;
 
 void ImportSampleDialog::OnFocus() {
+    // AU11_IMPORT_MODAL_STOP_ON_FOCUS: sample browser/listen/import/manage
+    // must not open while the sequencer is rendering project audio.
+    Player *player = Player::GetInstance();
+    if (player) {
+        if (player->IsRunning()) { player->Stop(); Trace::Log("AU11", "IMPORT_MODAL_STOP_PROJECT"); }
+        if (player->IsStreaming()) { player->StopStreaming(); Trace::Log("AU11", "IMPORT_MODAL_STOP_STREAM"); }
+    }
+    TimeService::GetInstance()->Sleep(80);
 	Path current(currentPath_) ;
 	setCurrentFolder(&current) ;
 	toInstr_=viewData_->currentInstrument_ ;
--- /tmp/au10z_srcfull/LGPT_PORT_U2_50_FINAL_MIXER_MASTER_SOURCE/sources/Application/Views/ModalDialogs/SampleManagerDialog.cpp	2026-07-10 00:31:57.000000000 +0000
+++ /mnt/data/U2_38AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/source_full/LGPT_PORT_U2_50_AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/sources/Application/Views/ModalDialogs/SampleManagerDialog.cpp	2026-07-10 01:20:45.260790756 +0000
@@ -7,6 +7,8 @@
 #include "Application/Instruments/SamplePool.h"
 #include "Application/Model/Song.h"
 #include "Application/Player/Player.h"
+#include "Services/Time/TimeService.h"
+#include "System/Console/Trace.h"
 #include "System/FileSystem/FileSystem.h"
 #include <stdarg.h>
 #include <stdio.h>
@@ -301,6 +303,14 @@
 }
 
 void SampleManagerDialog::OnFocus() {
+    // AU11_SAMPLE_MANAGER_STOP_ON_FOCUS: deleting/purging sample memory is modal
+    // and must happen with project playback stopped.
+    Player *player = Player::GetInstance();
+    if (player) {
+        if (player->IsRunning()) { player->Stop(); Trace::Log("AU11", "SAMPLE_MANAGER_STOP_PROJECT"); }
+        if (player->IsStreaming()) { player->StopStreaming(); Trace::Log("AU11", "SAMPLE_MANAGER_STOP_STREAM"); }
+    }
+    TimeService::GetInstance()->Sleep(80);
     clampSelection();
     isDirty_ = true;
 }
--- /tmp/au10z_srcfull/LGPT_PORT_U2_50_FINAL_MIXER_MASTER_SOURCE/sources/Application/Views/ModalDialogs/SampleChopperModal.cpp	2026-07-10 01:03:21.000000000 +0000
+++ /mnt/data/U2_38AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/source_full/LGPT_PORT_U2_50_AU11_USB_SAMPLER_CLEAN_FULL_SOURCE/sources/Application/Views/ModalDialogs/SampleChopperModal.cpp	2026-07-10 01:20:33.781171652 +0000
@@ -2902,7 +2902,7 @@
     drawStringAbs(0, 24, "R1+LR sample  L1+LR fast cursor", props);
     drawStringAbs(0, 25, "A cut/live Y del B play SELECT crop", props);
     drawStringAbs(0, 26, trimMode_ ? "R1+A crop  L2+Y del R1+X undo" : "R2+LR chop R2+UD release", props);
-    drawStringAbs(0, 27, "L2+A USB REC menu  L2+X load last", props);
+    drawStringAbs(0, 27, "USB REC: Instrument L1+LEFT  L2+X load", props);
     SetColor(CD_HILITE1);
     drawStringAbs(0, 28, trimMode_ ? "EDIT: A/B edge  Y start X end" : "SELECT edit  L1+R1 pitch R1+B back", props);
 }
@@ -2940,15 +2940,15 @@
     pitchMode_ = false;
     clearOverlayState();
     usbCaptureMenuActive_ = true;
-    usbCaptureRequireRelease_ = true;
+    usbCaptureRequireRelease_ = false; /* AU11_FAST_RECORD_MENU_NO_RELEASE_LAG */
     usbCaptureLastMask_ = 0;
-    usbCaptureOpenGuard_ = 0; // AU10Z_FAST_RECORD_MENU_NO_EVENT_GUARD
+    usbCaptureOpenGuard_ = 0; // AU11_FAST_RECORD_MENU_NO_RELEASE_LAG
     usbCaptureMenuSelection_ = 0;
     usbCaptureFilenameEdit_ = false;
     if (!usbCapturePendingName_[0]) snprintf(usbCapturePendingName_, sizeof(usbCapturePendingName_), "Sample_001.wav");
     usbCaptureFilenameCursor_ = 0;
     usbCaptureMonitor_ = true;
-    TreeFrogUac2Bridge_SetUsbMonitor(1);
+    TreeFrogUac2Bridge_SetUsbMonitor(1); /* AU11 monitor-on; UI no longer opens FIFO synchronously. */
     au10yRecordModalSentinel(1);
     setStatus("USB REC menu");
 #if defined(PLATFORM_TREEFROG)
@@ -3089,7 +3089,7 @@
     stopSamplePreview();
     sanitizeUsbCapturePendingName();
     usbCaptureMonitor_ = true;
-    TreeFrogUac2Bridge_SetUsbMonitor(1);
+    TreeFrogUac2Bridge_SetUsbMonitor(1); /* AU11 monitor-on; UI no longer opens FIFO synchronously. */
     char name[96];
     snprintf(name, sizeof(name), "%s", usbCapturePendingName_);
     std::string logical = "samples:"; logical += name;
@@ -3224,7 +3224,7 @@
         drawStringAbs(2, 24, "File opens full filename editor", props);
     }
     SetColor(CD_HILITE1);
-    drawStringAbs(2, 26, "FULL_DUPLEX: USB out+in; REC mutes USB out", props);
+    drawStringAbs(2, 26, "USB-C REC monitors input; USB out muted", props);
     props.invert_ = false; SetColor(CD_NORMAL);
 }
 
@@ -3354,25 +3354,15 @@
        a full project was running. First press now stops project playback or
        Chopper preview; second press opens the independent record screen. */
     if (l2 && a && !(left || right || up || down || b || x || y || l1 || r1 || r2 || select)) {
-        Player *player = Player::GetInstance();
-        if (player && player->IsRunning()) {
-            player->Stop();
-            stopSamplePreview();
-            clearPreviewPlaybackRange();
-            clearOverlayState();
-            setStatus("Project stopped; USB REC");
-            openUsbCaptureMenu();
-            DrawView(); publishOverlayState();
-            return;
-        }
-        if (playbackTriggered_ || previewActive_ || (player && player->IsStreaming())) {
-            stopSamplePreview();
-            setStatus("Preview stopped; press L2+A again");
-            isDirty_ = true;
-            DrawView(); publishOverlayState();
-            return;
-        }
-        openUsbCaptureMenu(); DrawView(); publishOverlayState(); return;
+        /* AU11_DISABLE_CHOPPER_L2A_USB_REC: USB-C RECORD is now an
+           Instrument-only hidden sampler route: Instrument + L1+LEFT (SCPI-R).
+           Keeping the old Chopper L2+A path caused users to enter record through
+           two modal layers and made stale AU10 scripts hard to diagnose. */
+        stopSamplePreview();
+        setStatus("USB REC: Instrument L1+LEFT");
+        isDirty_ = true;
+        DrawView(); publishOverlayState();
+        return;
     }
     if (l2 && x && !(left || right || up || down || a || b || y || l1 || r1 || r2 || select)) {
         importLastUsbCapture(); DrawView(); publishOverlayState(); return;
