diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/U2_47_DSP_MASTER_TEST_NOTES_ES.md LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/U2_47_DSP_MASTER_TEST_NOTES_ES.md
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/U2_47_DSP_MASTER_TEST_NOTES_ES.md	1970-01-01 00:00:00.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/U2_47_DSP_MASTER_TEST_NOTES_ES.md	2026-07-07 07:46:37.565676491 +0000
@@ -0,0 +1,92 @@
+# U2.47 TEST - Mixer pan, master stereo meter y commands/FX
+
+Base: U2.46 FINAL Phrase workflow.
+
+Estado: paquete de prueba. No marcar como estable hasta validar playback, export WAV y proyectos antiguos en SD.
+
+## Mixer / Master
+
+- Se agrega pan por track al modelo de Mixer.
+- El pan se guarda/restaura en el bloque `MIXER` de `lgptsav.dat` como atributo `PAN`.
+- Compatibilidad: proyectos anteriores cargan con pan centrado porque el atributo no existe y el valor por defecto es `0`.
+- En Mixer:
+  - `R1 + LEFT` mueve el pan del track seleccionado hacia la izquierda.
+  - `R1 + RIGHT` mueve el pan del track seleccionado hacia la derecha.
+  - `R1 + DOWN` centra el pan del track seleccionado.
+- La vista Mixer muestra una línea `PN` por canal:
+  - `C` = centro.
+  - `Lxx` = izquierda.
+  - `Rxx` = derecha.
+- Se agrega medidor stereo L/R del master con barras horizontales usando `/`.
+- También se dibuja un medidor stereo master en Project/Master.
+
+## Commands/FX
+
+La lista visible del selector de commands queda depurada para performance y uso común. Los commands legacy siguen existiendo para reproducción de proyectos antiguos, pero no todos aparecen en el selector principal.
+
+Commands visibles principales:
+
+- `ARPG` arpegio.
+- `CRSH` drive/crush.
+- `ECHO` delay por feedback.
+- `FBYP` bypass inmediato de filtro.
+- `FCUT` rampa de cutoff.
+- `FRES` rampa de resonancia.
+- `FLTR` filtro low pass legacy cutoff/resonance.
+- `HPF ` filtro high pass explícito.
+- `KILL` corta nota.
+- `LPF ` filtro low pass explícito.
+- `PAN ` paneo por command.
+- `PFIN` fine pitch.
+- `PTCH` pitch.
+- `RTRG` retrigger.
+- `RVRB` ambiente/reverb simple por feedback corto.
+- `STOP` stop song.
+- `TABL` dispara table.
+- `TMPO` tempo.
+
+## Formatos nuevos
+
+`LPF :aabb`
+
+- `aa` = cutoff.
+- `bb` = resonance.
+- Fuerza mezcla low pass.
+
+`HPF :aabb`
+
+- `aa` = cutoff.
+- `bb` = resonance.
+- Fuerza mezcla high pass aproximada usando el motor de filtro existente.
+
+`FBYP:----`
+
+- Bypassa el filtro del voice actual.
+- Cutoff full, resonance cero, sin rampa activa.
+
+`ECHO:aabb`
+
+- `aa` = tiempo/offset de feedback.
+- `bb` = wet/mix.
+- Wet limitado internamente para evitar runaway feedback.
+
+`RVRB:aabb`
+
+- `aa` = tamaño/offset corto.
+- `bb` = wet/mix.
+- Reverb simple tipo smear/ambience, no convolución.
+
+## Pendiente para prueba
+
+1. Verificar que proyectos U2.46 carguen con pan centrado.
+2. Verificar que el pan se escuche y se guarde al guardar proyecto.
+3. Verificar que Song WAV y Multitrack respeten el pan en export stereo.
+4. Verificar que stems sigan exportando sin rutas incorrectas.
+5. Probar commands `LPF`, `HPF`, `FBYP`, `ECHO`, `RVRB` en Phrase y Table.
+6. Confirmar que la lista depurada de commands no oculta algo indispensable para tu flujo.
+
+## Validación local
+
+- `VERIFY_U2_36_FINAL_PRE_OTG_SOURCE.sh`: OK.
+- Revisión de sintaxis local con `g++ -fsyntax-only` sobre archivos modificados: OK.
+- Build MIPS real no ejecutado en este entorno. El intento de build se detuvo antes de compilar por dependencia local ausente de Python/Pillow (`PIL`) en el generador de fuente. En tu WSL habitual debería compilar si ya venías compilando U2.46.
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Instruments/CommandList.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Instruments/CommandList.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Instruments/CommandList.cpp	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Instruments/CommandList.cpp	2026-07-07 07:42:40.346122281 +0000
@@ -5,30 +5,22 @@
 	I_CMD_NONE,
 	I_CMD_ARPG,
 	I_CMD_CRSH,
-	I_CMD_DLAY,
-	I_CMD_FBMX,
-	I_CMD_FBTN,
+	I_CMD_ECHO,
+	I_CMD_FBYP,
 	I_CMD_FCUT,
-	I_CMD_FLTR,
 	I_CMD_FRES,
-	I_CMD_GROV,
-	I_CMD_HOP,
-	I_CMD_IRTG,
+	I_CMD_FLTR,
+	I_CMD_HPF_,
 	I_CMD_KILL,
-	I_CMD_LEGA,
-	I_CMD_LPOF,
-	I_CMD_MDCC,
-	I_CMD_MDPG,
-	I_CMD_MVEL,
+	I_CMD_LPF_,
 	I_CMD_PAN_,
 	I_CMD_PFIN,
-	I_CMD_PLOF,
 	I_CMD_PTCH,
 	I_CMD_RTRG,
+	I_CMD_RVRB,
 	I_CMD_STOP,
 	I_CMD_TABL,
-	I_CMD_TMPO,
-	I_CMD_VOLM
+	I_CMD_TMPO
 } ;
 
 int CommandList::GetCount() { return sizeof(_all) / sizeof(FourCC); }
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Instruments/CommandList.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Instruments/CommandList.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Instruments/CommandList.h	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Instruments/CommandList.h	2026-07-07 07:42:40.345649490 +0000
@@ -19,6 +19,11 @@
 #define I_CMD_MVEL MAKE_FOURCC('M','V','E','L')
 #define I_CMD_PLOF MAKE_FOURCC('P','L','O','F')
 #define I_CMD_FLTR MAKE_FOURCC('F','L','T','R')
+#define I_CMD_LPF_ MAKE_FOURCC('L','P','F',' ')
+#define I_CMD_HPF_ MAKE_FOURCC('H','P','F',' ')
+#define I_CMD_FBYP MAKE_FOURCC('F','B','Y','P')
+#define I_CMD_ECHO MAKE_FOURCC('E','C','H','O')
+#define I_CMD_RVRB MAKE_FOURCC('R','V','R','B')
 #define I_CMD_TABL MAKE_FOURCC('T','A','B','L')
 #define I_CMD_CRSH MAKE_FOURCC('C','R','S','H')
 #define I_CMD_FCUT MAKE_FOURCC('F','C','U','T')
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Instruments/SampleInstrument.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Instruments/SampleInstrument.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Instruments/SampleInstrument.cpp	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Instruments/SampleInstrument.cpp	2026-07-07 07:42:40.347656161 +0000
@@ -374,6 +374,7 @@
 
 		rp->cutoff_=rp->baseFCut_=fl2fp(cutoff_->GetInt()/255.0f); 
 		rp->reso_=rp->baseFRes_=fl2fp(reso_->GetInt()/255.0f) ;
+		rp->filterMix_=filterMix_->GetInt() ;
 
 	// Init crush params
 
@@ -494,7 +495,7 @@
 
 		 bool hasUpdaters=!(rp->activeUpdaters_.empty()) ;
 
-		 int filterMix=filterMix_->GetInt() ;
+		 int filterMix=rp->filterMix_ ;
 		 FilterMode filterMode=(FilterMode)filterMode_->GetInt() ;
 		 bool filterBoost=(filterMode==FM_SCREAM) ;
 		 bool bassyFilter=(filterMode==FM_BASSY) ;
@@ -1234,6 +1235,34 @@
 			}
 			break ;
 
+		case I_CMD_LPF_:
+			{
+				float cut=(value>>8)/255.0f;
+				float res=(value&0xFF)/255.0f;
+				rp->filterMix_=0;
+				rp->cutoff_=rp->baseFCut_=fl2fp(cut);
+				rp->reso_=rp->baseFRes_=fl2fp(res);
+			}
+			break;
+		case I_CMD_HPF_:
+			{
+				float cut=(value>>8)/255.0f;
+				float res=(value&0xFF)/255.0f;
+				rp->filterMix_=255;
+				rp->cutoff_=rp->baseFCut_=fl2fp(cut);
+				rp->reso_=rp->baseFRes_=fl2fp(res);
+			}
+			break;
+		case I_CMD_FBYP:
+			{
+				rp->filterMix_=0;
+				rp->cutoff_=rp->baseFCut_=i2fp(1);
+				rp->reso_=rp->baseFRes_=0;
+				if (rp->cutRamp_.Enabled()) rp->cutRamp_.Disable();
+				if (rp->resRamp_.Enabled()) rp->resRamp_.Disable();
+			}
+			break;
+
 		case I_CMD_FCUT:
 			{
 				float target=float(value&0xFF)/255.0f ;
@@ -1302,6 +1331,28 @@
 			}
 			break ;
 
+		case I_CMD_ECHO:
+			{
+				float time=float(value>>8)/255.0f;
+				float wet=float(value&0xFF)/255.0f;
+				if (wet>0.70f) wet=0.70f;
+				rp->fbTun_=rp->baseFbTun_=fl2fp(time);
+				rp->fbMix_=rp->baseFbMix_=fl2fp(wet);
+			}
+			break;
+
+		case I_CMD_RVRB:
+			{
+				float size=float(value>>8)/255.0f;
+				float wet=float(value&0xFF)/255.0f;
+				if (wet>0.45f) wet=0.45f;
+				// Small feedback offsets create a compact ambience/reverb-like smear.
+				float time=0.18f+(size*0.32f);
+				rp->fbTun_=rp->baseFbTun_=fl2fp(time);
+				rp->fbMix_=rp->baseFbMix_=fl2fp(wet);
+			}
+			break;
+
 		case I_CMD_PTCH:
 			{
 				int pitch=(char)(value&0xFF) ; // number of semi tones
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Instruments/SampleRenderingParams.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Instruments/SampleRenderingParams.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Instruments/SampleRenderingParams.h	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Instruments/SampleRenderingParams.h	2026-07-07 07:42:40.343846435 +0000
@@ -42,6 +42,7 @@
 
 	fixed cutoff_ ; // filter cutoff
 	fixed reso_ ;   // filter reso
+	int filterMix_ ; // 0=lowpass, 255=highpass-style mix
 
 	fixed baseFbTun_ ;
 	fixed baseFbMix_ ;
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Mixer/MixerService.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Mixer/MixerService.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Mixer/MixerService.cpp	2026-07-07 05:27:24.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Mixer/MixerService.cpp	2026-07-07 07:41:05.237607240 +0000
@@ -462,6 +462,14 @@
     return out_->GetPlayedBufferPercentage() ;
 }
 
+int MixerService::GetMasterPeakLeft() const {
+    return out_ ? out_->GetLastPeakLeft() : 0 ;
+}
+
+int MixerService::GetMasterPeakRight() const {
+    return out_ ? out_->GetLastPeakRight() : 0 ;
+}
+
 void MixerService::toggleRendering(bool enable) {
     if (enable == isRendering_) return;
 
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Mixer/MixerService.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Mixer/MixerService.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Mixer/MixerService.h	2026-07-07 05:27:41.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Mixer/MixerService.h	2026-07-07 07:41:05.237418773 +0000
@@ -60,6 +60,8 @@
     void EndOfflineRendering();
     void RenderOfflineSlice();
     int GetPlayedBufferPercentage() ;
+    int GetMasterPeakLeft() const ;
+    int GetMasterPeakRight() const ;
 	
 	virtual void Execute(FourCC id,float value) ;
 
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Model/Mixer.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Model/Mixer.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Model/Mixer.cpp	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Model/Mixer.cpp	2026-07-07 07:41:05.231946501 +0000
@@ -19,6 +19,12 @@
 	return volume ;
 } ;
 
+int Mixer::clampPan(int pan) const {
+	if (pan<-100) return -100 ;
+	if (pan>100) return 100 ;
+	return pan ;
+} ;
+
 int Mixer::clampBus(int bus) const {
 	if (bus<0) return 0 ;
 	if (bus>9) return 9 ;
@@ -29,6 +35,7 @@
 	for (int i=0;i<SONG_CHANNEL_COUNT;i++) {
 		channelBus_[i]=i ;
 		channelVolume_[i]=100 ;
+		channelPan_[i]=0 ;
 	}
 } ;
 
@@ -57,12 +64,28 @@
 	SetChannelVolume(i,GetChannelVolume(i)+delta) ;
 } ;
 
+int Mixer::GetChannelPan(int i) {
+	i=clampChannel(i) ;
+	return clampPan(channelPan_[i]) ;
+} ;
+
+void Mixer::SetChannelPan(int i,int pan) {
+	i=clampChannel(i) ;
+	channelPan_[i]=clampPan(pan) ;
+} ;
+
+void Mixer::NudgeChannelPan(int i,int delta) {
+	i=clampChannel(i) ;
+	SetChannelPan(i,GetChannelPan(i)+delta) ;
+} ;
+
 void Mixer::SaveContent(TiXmlNode *node) {
 	for (int i=0;i<SONG_CHANNEL_COUNT;i++) {
 		TiXmlElement channel("CHANNEL") ;
 		channel.SetAttribute("INDEX",i) ;
 		channel.SetAttribute("BUS",GetBus(i)) ;
 		channel.SetAttribute("VOLUME",GetChannelVolume(i)) ;
+		channel.SetAttribute("PAN",GetChannelPan(i)) ;
 		node->InsertEndChild(channel) ;
 	}
 } ;
@@ -75,6 +98,7 @@
 		int index=-1 ;
 		int bus=0 ;
 		int volume=100 ;
+		int pan=0 ;
 		if (current->Attribute("INDEX",&index)) {
 			if (index>=0 && index<SONG_CHANNEL_COUNT) {
 				if (current->Attribute("BUS",&bus)) {
@@ -83,6 +107,9 @@
 				if (current->Attribute("VOLUME",&volume)) {
 					SetChannelVolume(index,volume) ;
 				}
+				if (current->Attribute("PAN",&pan)) {
+					SetChannelPan(index,pan) ;
+				}
 			}
 		}
 		current=current->NextSiblingElement("CHANNEL") ;
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Model/Mixer.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Model/Mixer.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Model/Mixer.h	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Model/Mixer.h	2026-07-07 07:41:05.231520896 +0000
@@ -19,15 +19,21 @@
 	void SetChannelVolume(int i,int volume) ;
 	void NudgeChannelVolume(int i,int delta) ;
 
+	int GetChannelPan(int i) ;
+	void SetChannelPan(int i,int pan) ;
+	void NudgeChannelPan(int i,int delta) ;
+
 	virtual void SaveContent(TiXmlNode *node) ;
 	virtual void RestoreContent(TiXmlElement *element);
 private:
 	int clampChannel(int i) const ;
 	int clampVolume(int volume) const ;
+	int clampPan(int pan) const ;
 	int clampBus(int bus) const ;
 
 	char channelBus_[SONG_CHANNEL_COUNT] ;
 	unsigned char channelVolume_[SONG_CHANNEL_COUNT] ;
+	signed char channelPan_[SONG_CHANNEL_COUNT] ;
 } ;	
 
 #endif
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/Player.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/Player.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/Player.cpp	2026-07-07 06:29:22.996053062 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/Player.cpp	2026-07-07 07:41:05.238492491 +0000
@@ -682,6 +682,14 @@
      return mixer_->Clipped() ;
 }
 
+int Player::GetMasterPeakLeft() {
+     return mixer_->GetMasterPeakLeft() ;
+}
+
+int Player::GetMasterPeakRight() {
+     return mixer_->GetMasterPeakRight() ;
+}
+
 bool Player::isPlayable(int row,int col,int chainPos) {
 
 	uchar *chain=viewData_->song_->data_+8*row+col ;
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/Player.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/Player.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/Player.h	2026-07-07 05:14:36.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/Player.h	2026-07-07 07:41:05.237912063 +0000
@@ -69,6 +69,8 @@
 	
 	bool IsRunning() ;
 	bool Clipped() ;
+	int GetMasterPeakLeft() ;
+	int GetMasterPeakRight() ;
 	
 	void ProcessCommands() ;
 	bool ProcessChannelCommand(int channel,FourCC cmd,ushort param) ;
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/PlayerChannel.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/PlayerChannel.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/PlayerChannel.cpp	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/PlayerChannel.cpp	2026-07-07 07:41:05.235849867 +0000
@@ -10,6 +10,7 @@
     instr_=0 ;
     muted_=false ;
 	volume_=100 ;
+	pan_=0 ;
 	mixBus_=0 ;
 	busIndex_=-1 ;
 }
@@ -49,6 +50,17 @@
            current++ ;
         }
      }
+     if (audible&&(pan_!=0)) {
+        int leftGain=(pan_>0)?(100-pan_):100 ;
+        int rightGain=(pan_<0)?(100+pan_):100 ;
+        fixed *current=buffer ;
+        int frames=samplecount ;
+        while (frames--) {
+           current[0]=(fixed)(((long long)current[0]*(long long)leftGain)/100LL) ;
+           current[1]=(fixed)(((long long)current[1]*(long long)rightGain)/100LL) ;
+           current+=2 ;
+        }
+     }
      return audible ;
    } else {
      return false ;
@@ -89,6 +101,12 @@
 	volume_=volume ;
 } ;
 
+void PlayerChannel::SetPan(int pan) {
+	if (pan<-100) pan=-100 ;
+	if (pan>100) pan=100 ;
+	pan_=pan ;
+} ;
+
 void PlayerChannel::Reset() {
 	if (mixBus_) {
 		mixBus_->Remove(*this) ;
@@ -96,5 +114,6 @@
 	mixBus_=0 ;
 	muted_=false ;
 	volume_=100 ;
+	pan_=0 ;
 	busIndex_=-1 ;
 } ;
\ No newline at end of file
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/PlayerChannel.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/PlayerChannel.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/PlayerChannel.h	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/PlayerChannel.h	2026-07-07 07:41:05.232255786 +0000
@@ -18,6 +18,7 @@
 	bool IsMuted() ;
 	void SetMixBus(int i) ;
 	void SetVolume(int volume) ;
+	void SetPan(int pan) ;
 	void Reset() ;
 private:
 	int index_ ;
@@ -25,6 +26,7 @@
 	bool muted_ ;
 	int busIndex_ ;
 	int volume_ ;
+	int pan_ ;
 	MixBus *mixBus_ ;
 } ;
 
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/PlayerMixer.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/PlayerMixer.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/PlayerMixer.cpp	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/PlayerMixer.cpp	2026-07-07 07:41:05.236312998 +0000
@@ -96,6 +96,16 @@
      return clipped_ ;
 }
 
+int PlayerMixer::GetMasterPeakLeft() {
+    MixerService *ms=MixerService::GetInstance() ;
+    return ms->GetMasterPeakLeft() ;
+}
+
+int PlayerMixer::GetMasterPeakRight() {
+    MixerService *ms=MixerService::GetInstance() ;
+    return ms->GetMasterPeakRight() ;
+}
+
 void PlayerMixer::Update(Observable &o,I_ObservableData *d) {
 
   // Notifies the player so that pattern data is processed
@@ -108,6 +118,7 @@
   for (int i=0;i<SONG_CHANNEL_COUNT;i++) {
     channel_[i]->SetMixBus(mixer->GetBus(i));
     channel_[i]->SetVolume(mixer->GetChannelVolume(i));
+    channel_[i]->SetPan(mixer->GetChannelPan(i));
   }
   MixerService *ms=MixerService::GetInstance();
   ms->SetPregain(project_->GetPregain());
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/PlayerMixer.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/PlayerMixer.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Player/PlayerMixer.h	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Player/PlayerMixer.h	2026-07-07 07:41:05.236670378 +0000
@@ -50,6 +50,8 @@
 	int GetStreamingEndFrame() ;
 
 	bool Clipped() ;
+	int GetMasterPeakLeft() ;
+	int GetMasterPeakRight() ;
 
 	void Update(Observable &o,I_ObservableData *d) ;
 	int GetPlayedBufferPercentage() ;   
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Utils/HelpLegend.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Utils/HelpLegend.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Utils/HelpLegend.h	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Utils/HelpLegend.h	2026-07-07 07:42:40.346584901 +0000
@@ -25,8 +25,8 @@
 			result[2].assign("from original pitch");
 			break;
 		case I_CMD_VOLM:
-			result[0].assign("VOLuMe:aabb");
-			result[1].assign("approach volume");
+			result[0].assign("VOLuMe:aabb legacy");
+			result[1].assign("use Phrase Vol instead");
 			break;
 		case I_CMD_PTCH:
 			result[0].assign("PiTCH:aabb");
@@ -72,9 +72,24 @@
 			result[2].assign("move rel bb chunks");
 			break;
 		case I_CMD_FLTR:
-			result[0].assign("FiLTer&Resonance:aabb");
-			result[1].assign("cutoff aa");
-			result[2].assign("resonance bb");
+			result[0].assign("FiLTer LP:aabb");
+			result[1].assign("cutoff aa resonance bb");
+			result[2].assign("legacy low pass");
+			break;
+		case I_CMD_LPF_:
+			result[0].assign("LowPass:aabb");
+			result[1].assign("cutoff aa resonance bb");
+			result[2].assign("explicit low pass");
+			break;
+		case I_CMD_HPF_:
+			result[0].assign("HighPass:aabb");
+			result[1].assign("cutoff aa resonance bb");
+			result[2].assign("explicit high pass");
+			break;
+		case I_CMD_FBYP:
+			result[0].assign("FilterBYP:----");
+			result[1].assign("bypass filter now");
+			result[2].assign("clears filter ramps");
 			break;
 		case I_CMD_TABL:
 			result[0].assign("TABLe:--bb");
@@ -112,17 +127,27 @@
 			result[1].assign("fine tune to ");
 			break;
 		case I_CMD_DLAY:
-			result[0].assign("DeLAY:--bb");
-			result[1].assign("delay bb tics");
-			result[2].assign("");
+			result[0].assign("DeLAY:--bb legacy");
+			result[1].assign("delays note trigger");
+			result[2].assign("use ECHO for audio delay");
 			break;
 		case I_CMD_FBMX:
 			result[0].assign("FeedBack MiX:aabb");
-			result[1].assign("feedback mix to");
+			result[1].assign("legacy feedback mix");
 			break;
 		case I_CMD_FBTN:
 			result[0].assign("FeedBack TuNe:aabb");
-			result[1].assign("feedback tune to");
+			result[1].assign("legacy delay time");
+			break;
+		case I_CMD_ECHO:
+			result[0].assign("ECHO:aabb");
+			result[1].assign("aa time bb wet");
+			result[2].assign("audio delay feedback");
+			break;
+		case I_CMD_RVRB:
+			result[0].assign("ReVeRB:aabb");
+			result[1].assign("aa size bb wet");
+			result[2].assign("short ambience smear");
 			break;
 		case I_CMD_STOP:
 			result[0].assign("STOP playing song");
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Views/MixerView.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Views/MixerView.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Views/MixerView.cpp	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Views/MixerView.cpp	2026-07-07 07:44:41.335226852 +0000
@@ -51,6 +51,16 @@
 	isDirty_=true ;
 }
 
+void MixerView::updatePan(int delta) {
+	Mixer::GetInstance()->NudgeChannelPan(viewData_->mixerCol_,delta) ;
+	isDirty_=true ;
+}
+
+void MixerView::centerPan() {
+	Mixer::GetInstance()->SetChannelPan(viewData_->mixerCol_,0) ;
+	isDirty_=true ;
+}
+
 void MixerView::toggleMute() {
 	UIController::GetInstance()->ToggleMute(viewData_->mixerCol_,viewData_->mixerCol_) ;
 	isDirty_=true ;
@@ -101,6 +111,18 @@
 			switchSoloMode() ;
 			return ;
 		}
+		if (mask&EPBM_LEFT) {
+			updatePan(-5) ;
+			return ;
+		}
+		if (mask&EPBM_RIGHT) {
+			updatePan(5) ;
+			return ;
+		}
+		if (mask&EPBM_DOWN) {
+			centerPan() ;
+			return ;
+		}
 		if (mask&EPBM_UP) {
 			ViewType vt=VT_SONG;
 			ViewEvent ve(VET_SWITCH_VIEW,&vt) ;
@@ -155,6 +177,43 @@
 	}
 }
 
+void MixerView::formatPan(int pan,char *buffer) {
+	if (pan==0) {
+		sprintf(buffer," C ") ;
+	} else if (pan<0) {
+		sprintf(buffer,"L%02d",-pan) ;
+	} else {
+		sprintf(buffer,"R%02d",pan) ;
+	}
+}
+
+void MixerView::drawMasterMeter(int x,int y) {
+	Player *player=Player::GetInstance() ;
+	GUITextProperties props ;
+	char line[32] ;
+	int l=player->GetMasterPeakLeft() ;
+	int r=player->GetMasterPeakRight() ;
+	int lb=(l*10+99)/100 ;
+	int rb=(r*10+99)/100 ;
+	if (lb>10) lb=10 ;
+	if (rb>10) rb=10 ;
+	for (int i=0;i<10;i++) {
+		line[i]=(i<lb)?'/':'-' ;
+	}
+	line[10]=0 ;
+	SetColor((l>=98)?CD_HILITE2:CD_NORMAL) ;
+	DrawString(x,y,"L",props) ;
+	DrawString(x+2,y,line,props) ;
+	for (int i=0;i<10;i++) {
+		line[i]=(i<rb)?'/':'-' ;
+	}
+	line[10]=0 ;
+	SetColor((r>=98)?CD_HILITE2:CD_NORMAL) ;
+	DrawString(x,y+1,"R",props) ;
+	DrawString(x+2,y+1,line,props) ;
+	SetColor(CD_NORMAL) ;
+}
+
 void MixerView::drawVolumeBar(int channel,int x,int y,int height) {
 	Mixer *mixer=Mixer::GetInstance() ;
 	Player *player=Player::GetInstance() ;
@@ -165,6 +224,7 @@
 	GUITextProperties props ;
 	char buffer[8] ;
 	char hex[3] ;
+	char panStr[4] ;
 
 	hex2char(channel,hex) ;
 	if (selected) {
@@ -200,9 +260,15 @@
 	DrawString(x-1,y+height+2,buffer,props) ;
 	props.invert_=false ;
 
+	formatPan(mixer->GetChannelPan(channel),panStr) ;
+	SetColor(selected?CD_HILITE2:CD_NORMAL) ;
+	props.invert_=selected ;
+	DrawString(x-1,y+height+3,panStr,props) ;
+	props.invert_=false ;
+
 	if (muted) {
 		SetColor(CD_HILITE2) ;
-		DrawString(x,y+height+3,"M",props) ;
+		DrawString(x,y+height+4,"M",props) ;
 	}
 }
 
@@ -229,6 +295,7 @@
 	SetColor(CD_NORMAL) ;
 	DrawString(0,y0,"CH",props) ;
 	DrawString(0,y0+height+2,"VL",props) ;
+	DrawString(0,y0+height+3,"PN",props) ;
 
 	for (int i=0;i<8;i++) {
 		drawVolumeBar(i,x0+i*dx,y0,height) ;
@@ -236,9 +303,10 @@
 
 	SetColor(CD_NORMAL) ;
 	props.invert_=false ;
-	DrawString(4,y0+height+4,"A+UP/DN x10  A+L/R x1",props) ;
-	DrawString(4,y0+height+5,"L/R channel  R+B mute",props) ;
-	DrawString(4,y0+height+6,"START play  R+A solo",props) ;
+	drawMasterMeter(28,y0+height+2) ;
+	DrawString(4,y0+height+5,"A+UP/DN x10  A+L/R x1",props) ;
+	DrawString(4,y0+height+6,"R+L/R pan  R+DN center",props) ;
+	DrawString(4,y0+height+7,"START play  R+B mute R+A solo",props) ;
 
 	drawNotes() ;
     
@@ -292,5 +360,6 @@
 	DrawString(pos._x,pos._y,strbuffer,props) ;
 
     drawNotes() ;
+	drawMasterMeter(28,GetAnchor()._y+14) ;
 
 } ;
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Views/MixerView.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Views/MixerView.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Views/MixerView.h	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Views/MixerView.h	2026-07-07 07:41:42.532777050 +0000
@@ -19,9 +19,13 @@
 	void onStop() ;
 	void updateCursor(int dx,int dy)  ;
 	void updateVolume(int delta) ;
+	void updatePan(int delta) ;
+	void centerPan() ;
 	void toggleMute() ;
 	void switchSoloMode() ;
 	void drawVolumeBar(int channel,int x,int y,int height) ;
+	void drawMasterMeter(int x,int y) ;
+	void formatPan(int pan,char *buffer) ;
 private:
 	const char *song_ ;
 
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Views/ProjectView.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Views/ProjectView.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Views/ProjectView.cpp	2026-07-07 05:14:53.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Views/ProjectView.cpp	2026-07-07 07:44:41.335864659 +0000
@@ -13,6 +13,7 @@
 #include "BaseClasses/UITempoField.h"
 #include "Services/Midi/MidiService.h"
 #include "System/System/System.h"
+#include "Application/Player/Player.h"
 
 #define ACTION_PURGE            MAKE_FOURCC('P','U','R','G')
 #define ACTION_SAVE             MAKE_FOURCC('S','A','V','E')
@@ -299,6 +300,29 @@
     };
 } ;
 
+void ProjectView::drawMasterMeter(int x,int y) {
+    Player *player=Player::GetInstance();
+    GUITextProperties props;
+    char line[16];
+    int l=player->GetMasterPeakLeft();
+    int r=player->GetMasterPeakRight();
+    int lb=(l*12+99)/100;
+    int rb=(r*12+99)/100;
+    if (lb>12) lb=12;
+    if (rb>12) rb=12;
+    for (int i=0;i<12;i++) line[i]=(i<lb)?'/':'-';
+    line[12]=0;
+    SetColor((l>=98)?CD_HILITE2:CD_NORMAL);
+    DrawString(x,y,"Master L",props);
+    DrawString(x+9,y,line,props);
+    for (int i=0;i<12;i++) line[i]=(i<rb)?'/':'-';
+    line[12]=0;
+    SetColor((r>=98)?CD_HILITE2:CD_NORMAL);
+    DrawString(x,y+1,"Master R",props);
+    DrawString(x+9,y+1,line,props);
+    SetColor(CD_NORMAL);
+}
+
 void ProjectView::DrawView() {
 
     Clear() ;
@@ -317,6 +341,7 @@
 
     FieldView::Redraw();
     drawMap();
+    drawMasterMeter(0,22);
 
     int currentMode = project_->GetRenderMode();
     if ((viewData_->renderMode_ != currentMode) && !MixerService::GetInstance()->IsRendering()) {
@@ -333,6 +358,10 @@
     View::EnableNotification();
 } ;
 
+void ProjectView::OnPlayerUpdate(PlayerEventType,unsigned int) {
+    drawMasterMeter(0,22);
+}
+
 void ProjectView::Update(Observable &,I_ObservableData *data) {
 
 	if (!hasFocus_) {
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Views/ProjectView.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Views/ProjectView.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Application/Views/ProjectView.h	2026-07-06 13:30:30.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Application/Views/ProjectView.h	2026-07-07 07:44:41.335567573 +0000
@@ -13,7 +13,7 @@
 
 	virtual void ProcessButtonMask(unsigned short mask,bool pressed) ;
 	virtual void DrawView() ;
-	virtual void OnPlayerUpdate(PlayerEventType,unsigned int) {} ;
+	virtual void OnPlayerUpdate(PlayerEventType,unsigned int) ;
 	virtual void OnFocus() {} ;
 
 	// Observer for action callback
@@ -26,6 +26,7 @@
 	void OnQuit() ;
 
 protected:
+	void drawMasterMeter(int x,int y) ;
 private:
   Project *project_;
   // Debug
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Services/Audio/AudioMixer.cpp LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Services/Audio/AudioMixer.cpp
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Services/Audio/AudioMixer.cpp	2026-07-07 05:15:48.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Services/Audio/AudioMixer.cpp	2026-07-07 07:41:05.237262794 +0000
@@ -17,6 +17,8 @@
     softclipGain_ = 0 ;
 	masterVolume_ = 100 ;
 	clipped_ = false ;
+	lastPeakLeft_ = 0 ;
+	lastPeakRight_ = 0 ;
 	
 	// Precalculate constant values for softclipping algorithm
 	softClipData_[0].alpha = 1.45f; // -1.5db (approx.)
@@ -117,6 +119,8 @@
      if (gotData) {
          fixed *c = buffer;
          float damp = pow((float)masterVolume_ / 100, 4.0f);
+         int peakL = 0;
+         int peakR = 0;
 
          if (volume_ != i2fp(1)) {
              for (int i = 0; i < samplecount * 2; i++) {
@@ -134,8 +138,20 @@
              sample = fl2fp(fp2fl(sample) * renderDamp);
              sample = hardClip(softClip(sample));
              sample = fl2fp(damp * fp2fl(sample));
+             int absSample = sample < 0 ? -sample : sample;
+             int peak = fp2i(absSample);
+             if ((i & 1) == 0) {
+                 if (peak > peakL) peakL = peak;
+             } else {
+                 if (peak > peakR) peakR = peak;
+             }
              *c++ = sample;
          }
+         lastPeakLeft_ = (peakL >= 32767) ? 100 : (peakL * 100 / 32767);
+         lastPeakRight_ = (peakR >= 32767) ? 100 : (peakR * 100 / 32767);
+     } else {
+         lastPeakLeft_ = 0;
+         lastPeakRight_ = 0;
      }
     if (enableRendering_&&writer_) {
 		if (!gotData) {
@@ -160,6 +176,9 @@
 
 bool AudioMixer::Clipped() { return clipped_; }
 
+int AudioMixer::GetLastPeakLeft() const { return lastPeakLeft_; }
+int AudioMixer::GetLastPeakRight() const { return lastPeakRight_; }
+
 fixed AudioMixer::hardClip(fixed sample) {
     if (sample > MAX_POSITIVE_FIXED || sample < MAX_NEGATIVE_FIXED) {
         clipped_ = true;
diff -ruN '--exclude=projects/buildTREEFROG' '--exclude=*.o' '--exclude=*.d' '--exclude=dist/lgpt_libretro.so' LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Services/Audio/AudioMixer.h LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Services/Audio/AudioMixer.h
--- LGPT_PORT_U2_46_FINAL_PHRASE_WORKFLOW_SOURCE/sources/Services/Audio/AudioMixer.h	2026-07-07 04:23:06.000000000 +0000
+++ LGPT_PORT_U2_47_DSP_MASTER_TEST_SOURCE/sources/Services/Audio/AudioMixer.h	2026-07-07 07:41:05.237020578 +0000
@@ -25,6 +25,8 @@
     virtual void SetSoftclip(int clip, int gain);
     virtual void SetMasterVolume(int volume) ;
 	virtual bool Clipped() ;
+	int GetLastPeakLeft() const ;
+	int GetLastPeakRight() const ;
 	
 private:
   fixed hardClip(fixed sample);
@@ -39,5 +41,7 @@
   int softclipGain_;
   int masterVolume_;
   bool clipped_;
+  int lastPeakLeft_;
+  int lastPeakRight_;
 } ;
 #endif
