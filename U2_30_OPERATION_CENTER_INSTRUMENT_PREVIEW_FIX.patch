--- sources/Application/Views/ModalDialogs/SampleChopperModal.cpp	2026-07-04 15:53:00.570394867 +0000
+++ sources/Application/Views/ModalDialogs/SampleChopperModal.cpp	2026-07-04 16:04:44.800131470 +0000
@@ -961,6 +961,8 @@
 // TREEFROG_U2_28_CHOPPER_PANEL_INSTRUMENT_PREVIEW_FIX
 // U2.29: reflows Pitch/Env below sample info, hard-disables waveform overlay before drawing, removes Scope inline help.
 // TREEFROG_U2_29_CHOPPER_PANEL2_INSTRUMENT_PREVIEW_FIX
+// U2.30: centers operation completion overlay and fixes Instrument-screen sample preview after Chopper edits.
+// TREEFROG_U2_30_OPERATION_CENTER_INSTRUMENT_PREVIEW_FIX
 bool SampleChopperModal::hasPitchEnvelopeChange() const {
     return pitchSemitones_ != 0 || pitchAttackMs_ != 0 || pitchReleaseMs_ != 0 || pitchSustainPercent_ != 100;
 }
@@ -1834,7 +1836,7 @@
     SetColor(CD_HILITE1);
     props.invert_ = true;
     drawStringAbs(x, y + 0,  "+------------------------------------+", props);
-    snprintf(line, sizeof(line), "PITCH/ENV U2.29");
+    snprintf(line, sizeof(line), "PITCH/ENV U2.30");
     snprintf(msg, sizeof(msg), "| %-34.34s |", line);
     drawStringAbs(x, y + 1, msg, props);
     snprintf(line, sizeof(line), "I%02X S%02X C%02d/%02d", instrumentIndex_, sampleIndex_, selectedChop_ + 1, (boundaryCount_ > 1 ? boundaryCount_ - 1 : 1));
@@ -2505,7 +2507,7 @@
     for (int y = 2; y < 22; y++) { drawStringAbs(0, y, "|", props); drawStringAbs(39, y, "|", props); }
     drawStringAbs(0, 22, "+--------------------------------------+", props);
     SetColor(CD_HILITE2);
-    drawStringAbs(2, 2, "Graphical Chopper U2.29", props);
+    drawStringAbs(2, 2, "Graphical Chopper U2.30", props);
 }
 
 void SampleChopperModal::drawSampleInfo(GUITextProperties &props) {
@@ -2575,23 +2577,24 @@
 void SampleChopperModal::drawOperationOverlay(GUITextProperties &props) {
     if (!operationActive_) return;
     char msg[64];
+    const int x = 3;
+    const int y = 10;
     SetColor(CD_HILITE1);
     props.invert_ = true;
-    drawStringAbs(2, 9,  "+------------------------------------+", props);
-    snprintf(msg, sizeof(msg), "| %-34s |", operationMessage_);
-    msg[38] = 0;
-    drawStringAbs(2, 10, msg, props);
-    snprintf(msg, sizeof(msg), "|              %3d%%                  |", operationPercent_);
-    msg[38] = 0;
-    drawStringAbs(2, 11, msg, props);
+    drawStringAbs(x, y + 0, "+--------------------------------+", props);
+    snprintf(msg, sizeof(msg), "| %-30.30s |", operationMessage_);
+    drawStringAbs(x, y + 1, msg, props);
+    snprintf(msg, sizeof(msg), "|              %3d%%              |", operationPercent_);
+    msg[34] = 0;
+    drawStringAbs(x, y + 2, msg, props);
     if (operationPercent_ >= 100) {
-        drawStringAbs(2, 12, "|                 OK                 |", props);
-        drawStringAbs(2, 13, "|        Press A to continue         |", props);
+        drawStringAbs(x, y + 3, "|              OK                |", props);
+        drawStringAbs(x, y + 4, "|      Press A to continue       |", props);
     } else {
-        drawStringAbs(2, 12, "|            Please wait             |", props);
-        drawStringAbs(2, 13, "|         Processing sample          |", props);
+        drawStringAbs(x, y + 3, "|          Please wait           |", props);
+        drawStringAbs(x, y + 4, "|       Processing sample        |", props);
     }
-    drawStringAbs(2, 14, "+------------------------------------+", props);
+    drawStringAbs(x, y + 5, "+--------------------------------+", props);
     props.invert_ = false;
 }
 
--- sources/Application/Views/InstrumentView.cpp	2026-07-04 15:53:00.570809107 +0000
+++ sources/Application/Views/InstrumentView.cpp	2026-07-04 16:04:44.800586846 +0000
@@ -2,6 +2,7 @@
 #include "Application/Instruments/MidiInstrument.h"
 #include "Application/Instruments/SampleInstrument.h"
 #include "Application/Instruments/SamplePool.h"
+#include "Application/Instruments/WavFile.h"
 #include "Application/Model/Config.h"
 #include "Application/Player/Player.h"
 #include "BaseClasses/UIBigHexVarField.h"
@@ -286,7 +287,7 @@
 } ;
 
 void InstrumentView::previewCurrentSampleInstrument() {
-    // TREEFROG_U2_29_INSTRUMENT_B_PREVIEW_PATH_FIX
+    // TREEFROG_U2_30_INSTRUMENT_B_PREVIEW_STREAM_FIX
     if (getInstrumentType() != IT_SAMPLE) return;
 
     int i = viewData_->currentInstrument_;
@@ -297,28 +298,55 @@
 
     SampleInstrument *instrument = (SampleInstrument *)raw;
     Variable *sampleVar = instrument->FindVariable(SIP_SAMPLE);
-    if (!sampleVar || sampleVar->GetInt() == NO_SAMPLE) return;
+    int sampleIndex = sampleVar ? sampleVar->GetInt() : NO_SAMPLE;
+    if (sampleIndex == NO_SAMPLE) return;
 
     const char *name = instrument->GetFileName();
+    SamplePool *pool = SamplePool::GetInstance();
+    if ((!name || !name[0]) && pool && sampleIndex >= 0 && sampleIndex < pool->GetNameListSize()) {
+        char **names = pool->GetNameList();
+        if (names) name = names[sampleIndex];
+    }
     if (!name || !name[0]) return;
 
-    int size = instrument->GetSampleSize();
+    char logicalPath[256];
+    snprintf(logicalPath, sizeof(logicalPath), "samples:%s", name);
+    logicalPath[sizeof(logicalPath) - 1] = 0;
+    Path path(logicalPath);
+
+    int fileSize = 0;
+    WavFile *wav = WavFile::Open(path.GetPath().c_str());
+    if (wav) {
+        fileSize = wav->GetSize(-1);
+        delete wav;
+    }
+
+    int sourceSize = instrument->GetSampleSize();
+    int size = fileSize > 1 ? fileSize : sourceSize;
+    if (size <= 1) {
+        /* Last-resort path: allow the streamer to open the WAV itself. This keeps
+           Instrument preview usable even when SamplePool metadata is stale after
+           an in-place Chopper edit. */
+        Player::GetInstance()->StopStreaming();
+        Player::GetInstance()->StartStreaming(path);
+        isDirty_ = true;
+        return;
+    }
+
     Variable *vStart = instrument->FindVariable(SIP_START);
     Variable *vEnd = instrument->FindVariable(SIP_END);
     int start = vStart ? vStart->GetInt() : 0;
     int end = vEnd ? vEnd->GetInt() : size;
 
-    if (size <= 1) return;
     if (start < 0 || start >= size) start = 0;
     if (end <= start || end > size) end = size;
 
-    char logicalPath[256];
-    snprintf(logicalPath, sizeof(logicalPath), "samples:%s", name);
-    logicalPath[sizeof(logicalPath) - 1] = 0;
-
-    Path path(logicalPath);
     Player::GetInstance()->StopStreaming();
-    Player::GetInstance()->StartStreamingRangeAt(path, start, end - 1);
+    if (start <= 0 && end >= size) {
+        Player::GetInstance()->StartStreaming(path);
+    } else {
+        Player::GetInstance()->StartStreamingRangeAt(path, start, end - 1);
+    }
     isDirty_ = true;
 }
 
@@ -328,8 +356,21 @@
 
 	isDirty_=false ;
 
-    // U2.29: handle plain B preview before FieldView consumes/updates focus state.
-    if (mask == EPBM_B && getInstrumentType() == IT_SAMPLE) {
+    // U2.30: handle plain B preview before FieldView consumes/updates focus state.
+    bool plainBPreview = (mask & EPBM_B) &&
+        !(mask & (EPBM_LEFT | EPBM_RIGHT | EPBM_UP | EPBM_DOWN |
+                  EPBM_A | EPBM_X | EPBM_Y | EPBM_L | EPBM_R |
+                  EPBM_L2 | EPBM_R2 | EPBM_SELECT | EPBM_START));
+    bool stopPreview = (mask & EPBM_L2) && (mask & EPBM_B) &&
+        !(mask & (EPBM_LEFT | EPBM_RIGHT | EPBM_UP | EPBM_DOWN |
+                  EPBM_A | EPBM_X | EPBM_Y | EPBM_L | EPBM_R |
+                  EPBM_R2 | EPBM_SELECT | EPBM_START));
+    if (stopPreview && getInstrumentType() == IT_SAMPLE) {
+        Player::GetInstance()->StopStreaming();
+        isDirty_ = true;
+        return;
+    }
+    if (plainBPreview && getInstrumentType() == IT_SAMPLE) {
         previewCurrentSampleInstrument();
         return;
     }
