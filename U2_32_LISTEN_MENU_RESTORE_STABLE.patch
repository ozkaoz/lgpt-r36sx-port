--- sources/Application/Views/InstrumentView.cpp	2026-07-04 16:16:05.400118926 +0000
+++ sources/Application/Views/InstrumentView.cpp	2026-07-04 16:29:26.043783600 +0000
@@ -2,7 +2,6 @@
 #include "Application/Instruments/MidiInstrument.h"
 #include "Application/Instruments/SampleInstrument.h"
 #include "Application/Instruments/SamplePool.h"
-#include "Application/Instruments/WavFile.h"
 #include "Application/Model/Config.h"
 #include "Application/Player/Player.h"
 #include "BaseClasses/UIBigHexVarField.h"
@@ -10,13 +9,11 @@
 #include "BaseClasses/UINoteVarField.h"
 #include "BaseClasses/UIStaticField.h"
 #include "Foundation/Variables/Variable.h"
-#include "Services/Time/TimeService.h"
 #include "ModalDialogs/ImportSampleDialog.h"
 #include "ModalDialogs/SampleChopperModal.h"
 #include "ModalDialogs/MessageBox.h"
 #include "System/System/System.h"
 #include "System/FileSystem/FileSystem.h"
-#include <string>
 
 InstrumentView::InstrumentView(GUIWindow &w,ViewData *data):FieldView(w,data) {
 
@@ -288,125 +285,15 @@
 	isDirty_=true ;
 } ;
 
-bool InstrumentView::buildInstrumentPreviewWav(const Path &sourcePath, int start, int end, std::string &logicalPath) {
-    // TREEFROG_U2_31_INSTRUMENT_LISTEN_RESTORE_STABLE
-    logicalPath = "samples:__u2_instrument_preview.wav";
-
-    WavFile *wav = WavFile::Open(sourcePath.GetPath().c_str());
-    if (!wav) return false;
-
-    int size = wav->GetSize(-1);
-    int channels = wav->GetChannelCount(-1);
-    int rate = wav->GetSampleRate(-1);
-    if (size <= 0 || channels <= 0 || rate <= 0) {
-        delete wav;
-        return false;
-    }
-
-    if (start < 0 || start >= size) start = 0;
-    if (end <= start || end > size) end = size;
-    int frames = end - start;
-    if (frames <= 0) frames = size;
-
-    if (!wav->GetBuffer(start, frames)) {
-        delete wav;
-        return false;
-    }
-
-    short *buffer = (short *)wav->GetSampleBuffer(-1);
-    if (!buffer) {
-        delete wav;
-        return false;
-    }
-
-    bool ok = wav->ReplaceBuffer(buffer, frames, channels, rate);
-    if (ok) ok = wav->SaveBufferToPath(logicalPath.c_str());
-    delete wav;
-    return ok;
-}
-
-void InstrumentView::previewCurrentSampleInstrument() {
-    // TREEFROG_U2_31_INSTRUMENT_LISTEN_RESTORE_STABLE
-    if (getInstrumentType() != IT_SAMPLE) return;
-
-    int i = viewData_->currentInstrument_;
-    InstrumentBank *bank = viewData_->project_->GetInstrumentBank();
-    if (!bank) return;
-    I_Instrument *raw = bank->GetInstrument(i);
-    if (!raw || raw->GetType() != IT_SAMPLE) return;
-
-    SampleInstrument *instrument = (SampleInstrument *)raw;
-    Variable *sampleVar = instrument->FindVariable(SIP_SAMPLE);
-    int sampleIndex = sampleVar ? sampleVar->GetInt() : NO_SAMPLE;
-    if (sampleIndex == NO_SAMPLE) return;
-
-    const char *name = instrument->GetFileName();
-    SamplePool *pool = SamplePool::GetInstance();
-    if ((!name || !name[0]) && pool && sampleIndex >= 0 && sampleIndex < pool->GetNameListSize()) {
-        char **names = pool->GetNameList();
-        if (names) name = names[sampleIndex];
-    }
-    if (!name || !name[0]) return;
-
-    char logicalPath[256];
-    snprintf(logicalPath, sizeof(logicalPath), "samples:%s", name);
-    logicalPath[sizeof(logicalPath) - 1] = 0;
-    Path sourcePath(logicalPath);
-
-    int size = instrument->GetSampleSize();
-    WavFile *wav = WavFile::Open(sourcePath.GetPath().c_str());
-    if (wav) {
-        int fileSize = wav->GetSize(-1);
-        if (fileSize > 1) size = fileSize;
-        delete wav;
-    }
-    if (size <= 1) size = instrument->GetSampleSize();
-    if (size <= 1) return;
-
-    Variable *vStart = instrument->FindVariable(SIP_START);
-    Variable *vEnd = instrument->FindVariable(SIP_END);
-    int start = vStart ? vStart->GetInt() : 0;
-    int end = vEnd ? vEnd->GetInt() : size;
-    if (start < 0 || start >= size) start = 0;
-    if (end <= start || end > size) end = size;
-
-    /* U2.31: Instrument-listen now uses the same stable stream path used by
-       ImportSampleDialog/Chopper previews, but avoids the stale in-place edited
-       sample handle by rendering the selected range to an internal preview WAV
-       first.  This fixes the R36S case where Chopper Pitch/Env preview works,
-       but returning to Instrument and pressing B produces silence. */
-    std::string previewLogicalPath;
-    Path streamPath(sourcePath);
-    int streamStart = start;
-    int streamEnd = end - 1;
-    if (buildInstrumentPreviewWav(sourcePath, start, end, previewLogicalPath)) {
-        streamPath = Path(previewLogicalPath.c_str());
-        streamStart = 0;
-        streamEnd = end - start - 1;
-        if (streamEnd < 0) streamEnd = 0;
-    }
-
-    Player::GetInstance()->StopStreaming();
-    TimeService::GetInstance()->Sleep(80);
-    if (streamStart <= 0 && streamEnd >= size - 1 && previewLogicalPath.empty()) {
-        Player::GetInstance()->StartStreaming(streamPath);
-    } else {
-        Player::GetInstance()->StartStreamingRangeAt(streamPath, streamStart, streamEnd);
-    }
-    isDirty_ = true;
-}
-
 void InstrumentView::ProcessButtonMask(unsigned short mask,bool pressed) {
 
 	if (!pressed) return ;
 
 	isDirty_=false ;
 
-    // U2.30: handle plain B preview before FieldView consumes/updates focus state.
-    bool plainBPreview = (mask & EPBM_B) &&
-        !(mask & (EPBM_LEFT | EPBM_RIGHT | EPBM_UP | EPBM_DOWN |
-                  EPBM_A | EPBM_X | EPBM_Y | EPBM_L | EPBM_R |
-                  EPBM_L2 | EPBM_R2 | EPBM_SELECT | EPBM_START));
+    // TREEFROG_U2_32_LISTEN_MENU_RESTORE_STABLE
+    // Restore stable semantics: plain B in Instrument is not listen/preview.
+    // L2+B remains a hard stop for any sample preview started from Listen/Import.
     bool stopPreview = (mask & EPBM_L2) && (mask & EPBM_B) &&
         !(mask & (EPBM_LEFT | EPBM_RIGHT | EPBM_UP | EPBM_DOWN |
                   EPBM_A | EPBM_X | EPBM_Y | EPBM_L | EPBM_R |
@@ -416,10 +303,6 @@
         isDirty_ = true;
         return;
     }
-    if (plainBPreview && getInstrumentType() == IT_SAMPLE) {
-        previewCurrentSampleInstrument();
-        return;
-    }
 
 	if (viewMode_==VM_NEW) {
 		if (mask==EPBM_A) {
@@ -492,12 +375,6 @@
 
 	// B Modifier
 
-    if ((mask == EPBM_B) && getInstrumentType() == IT_SAMPLE) {
-        // TREEFROG_U2_31_INSTRUMENT_LISTEN_RESTORE_STABLE fallback after FieldView path.
-        previewCurrentSampleInstrument();
-        return;
-    }
-
     if (mask & EPBM_B) {
         if (mask&EPBM_LEFT) warpToNext(-1) ;
 		if (mask&EPBM_RIGHT) warpToNext(+1);
--- sources/Application/Views/InstrumentView.h	2026-07-04 16:15:24.295766619 +0000
+++ sources/Application/Views/InstrumentView.h	2026-07-04 16:29:26.044363223 +0000
@@ -6,7 +6,6 @@
 #include "Foundation/Observable.h"
 #include "ViewData.h"
 #include "System/FileSystem/FileSystem.h"
-#include <string>
 
 class InstrumentView: public FieldView, public I_Observer {
 public:
@@ -20,8 +19,6 @@
 
 protected:
 	void warpToNext(int offset) ;
-	bool buildInstrumentPreviewWav(const Path &sourcePath, int start, int end, std::string &logicalPath) ;
-	void previewCurrentSampleInstrument() ;
 	void onInstrumentChange() ;
 	void fillSampleParameters() ;
 	void fillMidiParameters() ;
--- sources/Application/Views/ModalDialogs/ImportSampleDialog.cpp	2026-07-04 13:46:38.477212393 +0000
+++ sources/Application/Views/ModalDialogs/ImportSampleDialog.cpp	2026-07-04 16:28:07.512173210 +0000
@@ -2,6 +2,8 @@
 #include "ImportSampleDialog.h"
 #include "Application/Instruments/SamplePool.h"
 #include "Application/Instruments/SampleInstrument.h"
+#include "Application/Instruments/WavFile.h"
+#include "Services/Time/TimeService.h"
 #include "System/Console/Trace.h"
 #include <stdarg.h>
 #include <stdio.h>
@@ -162,9 +164,53 @@
 	toInstr_=viewData_->currentInstrument_ ;
 } ;
 
+bool ImportSampleDialog::buildListenPreviewWav(Path &element, std::string &logicalPath) {
+    // TREEFROG_U2_32_LISTEN_MENU_RESTORE_STABLE
+    logicalPath = "samples:__u2_listen_preview.wav";
+    if (element.IsDirectory()) return false;
+
+    WavFile *wav = WavFile::Open(element.GetPath().c_str());
+    if (!wav) return false;
+
+    int size = wav->GetSize(-1);
+    int channels = wav->GetChannelCount(-1);
+    int rate = wav->GetSampleRate(-1);
+    if (size <= 0 || channels <= 0 || rate <= 0) {
+        delete wav;
+        return false;
+    }
+
+    if (!wav->GetBuffer(0, size)) {
+        delete wav;
+        return false;
+    }
+
+    short *buffer = (short *)wav->GetSampleBuffer(-1);
+    if (!buffer) {
+        delete wav;
+        return false;
+    }
+
+    bool ok = wav->ReplaceBuffer(buffer, size, channels, rate);
+    if (ok) ok = wav->SaveBufferToPath(logicalPath.c_str());
+    delete wav;
+    return ok;
+}
+
 void ImportSampleDialog::preview(Path &element) {
+    // TREEFROG_U2_32_LISTEN_MENU_RESTORE_STABLE
 	if (!element.IsDirectory()) {
-		Player::GetInstance()->StartStreaming(element) ;
+        std::string previewPath;
+        Player::GetInstance()->StopStreaming();
+        TimeService::GetInstance()->Sleep(60);
+        if (buildListenPreviewWav(element, previewPath)) {
+            Path path(previewPath.c_str());
+            Player::GetInstance()->StartStreaming(path);
+            setStatusMessage("Listen preview");
+        } else {
+            Player::GetInstance()->StartStreaming(element);
+            setStatusMessage("Listen direct");
+        }
 	}
 }
 
@@ -200,6 +246,16 @@
 
 	if (!pressed) return ;
 
+    // TREEFROG_U2_32_LISTEN_MENU_RESTORE_STABLE
+    // In Listen/Import, A on Listen starts preview; plain B does not preview.
+    // L2+B is the explicit stop combination, matching Chopper/PitchEnv.
+    if ((mask & EPBM_L2) && (mask & EPBM_B)) {
+        endPreview();
+        setStatusMessage("Preview stopped");
+        isDirty_ = true;
+        return ;
+    }
+
 	if (mask&EPBM_B) {
 		if (mask&EPBM_UP) warpToNextSample(-LIST_SIZE) ;
 		if (mask&EPBM_DOWN) warpToNextSample(LIST_SIZE) ;
--- sources/Application/Views/ModalDialogs/ImportSampleDialog.h	2026-07-04 13:46:38.477304649 +0000
+++ sources/Application/Views/ModalDialogs/ImportSampleDialog.h	2026-07-04 16:28:07.512536975 +0000
@@ -22,6 +22,7 @@
 	void import(Path &element) ;
 	void preview(Path &element) ;
 	void endPreview() ;
+    bool buildListenPreviewWav(Path &element, std::string &logicalPath) ;
 private:
 	Path *getImportElement();
 	bool isSampleLibRoot();
--- sources/Application/Views/ModalDialogs/SampleChopperModal.cpp	2026-07-04 16:15:24.296863544 +0000
+++ sources/Application/Views/ModalDialogs/SampleChopperModal.cpp	2026-07-04 16:28:27.489282116 +0000
@@ -964,6 +964,8 @@
 // U2.30: centers operation completion overlay and fixes Instrument-screen sample preview after Chopper edits.
 // TREEFROG_U2_30_OPERATION_CENTER_INSTRUMENT_PREVIEW_FIX
 // TREEFROG_U2_31_INSTRUMENT_LISTEN_RESTORE_STABLE
+// U2.32: restores stable Listen/Import semantics: Instrument B does not preview; A on Listen previews; L2+B stops.
+// TREEFROG_U2_32_LISTEN_MENU_RESTORE_STABLE
 bool SampleChopperModal::hasPitchEnvelopeChange() const {
     return pitchSemitones_ != 0 || pitchAttackMs_ != 0 || pitchReleaseMs_ != 0 || pitchSustainPercent_ != 100;
 }
@@ -1837,7 +1839,7 @@
     SetColor(CD_HILITE1);
     props.invert_ = true;
     drawStringAbs(x, y + 0,  "+------------------------------------+", props);
-    snprintf(line, sizeof(line), "PITCH/ENV U2.31");
+    snprintf(line, sizeof(line), "PITCH/ENV U2.32");
     snprintf(msg, sizeof(msg), "| %-34.34s |", line);
     drawStringAbs(x, y + 1, msg, props);
     snprintf(line, sizeof(line), "I%02X S%02X C%02d/%02d", instrumentIndex_, sampleIndex_, selectedChop_ + 1, (boundaryCount_ > 1 ? boundaryCount_ - 1 : 1));
@@ -2508,7 +2510,7 @@
     for (int y = 2; y < 22; y++) { drawStringAbs(0, y, "|", props); drawStringAbs(39, y, "|", props); }
     drawStringAbs(0, 22, "+--------------------------------------+", props);
     SetColor(CD_HILITE2);
-    drawStringAbs(2, 2, "Graphical Chopper U2.31", props);
+    drawStringAbs(2, 2, "Graphical Chopper U2.32", props);
 }
 
 void SampleChopperModal::drawSampleInfo(GUITextProperties &props) {
