# CHOPPER_BACON14_DIFF.md
# Bacon 1.4 Visual Contract Restoration

Generated: 2026-08-21 21:23
HEAD before: 6d99b97 (docs checkpoint STABLE RC dbaed387ff72)
HEAD after fix: WORKING TREE (View/AppWindow hardening + pitch overlay revert)
Old core: dbaed387ff72d309512256bd3c17ec33106ece39823991a52e33a19f66b25f82 (1.4M)
New core: 91f2a4570747a2cc... (1.4M) build 2026-08-21 21:23

| COMPONENT | BACON 1.4 (d2a897a / 48318a0) | CURRENT BEFORE (6d99b97) | CURRENT AFTER | RATIONALE |
|---|---|---|---|---|
| View::ClearRect | `GUIRect rect(x,y,x+w,y+h); w_.ClearRect(rect);` no clipping, x,y,w,h invariant | Hardened: `x0=x,y0=y,x1=x+w,y1=y+h; if(x0<0)x0=0; if(y0<0)y0=0; if(x1>40)x1=40; if(y1>30)y1=30; if(x0>=x1||y0>=y1)return;` with clipping but only partial clamp (x0<0, y0<0, x1>40, y1>30) no w<=0 early, no overflow guard | Hardened CORRECT: `if(w<=0||h<=0)return; long long x0=x,y0=y,x1=(long long)x+w,y1=(long long)y+h; if(x0<0)x0=0; if(x0>40)x0=40; if(y0<0)y0=0; if(y0>30)y0=30; if(x1<0)x1=0; if(x1>40)x1=40; if(y1<0)y1=0; if(y1>30)y1=30; if(x1<=x0||y1<=y0)return; GUIRect((int)x0,(int)y0,(int)x1,(int)y1)` | Preserve x,y,w,h semantics (x+w,y+h) while clipping to 40x30, early w/h zero check, overflow safe via long long, full clamp both x0/y0 and x1/y1. BEFORE was already correct for w interpretation but missing low clamp for x1/y1 and overflow. AFTER fixes spec 5. |
| AppWindow::ClearRect | `char *st=_charScreen+x+40*y; for(i<h) for(j<w) *st++=' '; st+=40-w;` no clipping -> overflow possible | Same as golden, no hardening (still vulnerable) | Hardened: same long long clipping as View, early w<=0, full clamp, derive clipped x,y,w,h before pointer arithmetic | View protects callers, AppWindow as last barrier (spec 6). Before had no protection; after prevents overflow when View caller passes unclipped 320x240 etc. |
| showOperationProgress | `operationActive=true; clamp; snprintf; g_chopperOperationActive=1; ComposeOperationStatus; setStatus; DrawView; publishOverlayState; w_.Flush; TreeFrogForceVideoRefresh; if(percent<100)Sleep(90); isDirty=true` | Identical to golden (already correct) | Unchanged (identical) | Preserved golden pacing (Sleep 90) spec 8/9. No optimization. |
| clearOperationProgress | `operationActive=false; percent=0; message=0; combo=0; g_chopperOperationActive=0; isDirty=true` | Identical | Unchanged | |
| drawOperationOverlay | `tf_rect(0,64,320,112, dark); tf_rect frames 63,176,64,319; ClearRect(0,8,40,14); DrawCenteredTitle OPERATION 8; MakeCenteredMenuLayout ... Status + percent + hints A close/L1+X undo/R1+X redo vs Processing...` | Identical | Unchanged | Critical: ClearRect(0,8,40,14) must be 40x14 rows 8..21 (560 cells) not 40x6. AFTER hardening preserves this. Panel solid dark y=64..176 (112). |
| drawPitchScreen | T4: `tf_rect(0,60,320,116,dark) y=60..175 h116; frames 59,176,60,319; MakeCenteredMenuLayout(7,11,10,2,40,8,22); PITCH/ENV title; header; 6 params; hints;` D2a: y=59 h118 | BEFORE had T4 correct geometry 60,116 but ALSO added fullscreen direct-FB overlay via TreeFrogChopperOverlayDraw (0,0,320,240 tf_rect + tf_text) that overwrote char screen, violated y=60..175 limit, tapped status/hints (V53) | AFTER reverted: removed fullscreen direct-FB pitch overlay (g_chopperPitch* vars, tf_text, publishing). Kept T4 geometry 60,116 via drawPitchScreen + char DrawString. Preserved safety checks (sampleIndex bounds, source null) and hasWaveform gate removal. | Spec 12: pitch panel must be approx y=60..175 never 239, status rows 23-25 intact. BEFORE fullscreen violated. AFTER restores Bacon1.4 T4 visual while keeping safety. |
| previewPitchSetting | `show 5% -> prepare -> show 65% -> StopStreamingAndRelease -> writePreview -> show 90% -> StartStreamingRangeAt -> Deactivate preview -> clearOperationProgress -> setStatus Preview Chop/Sample P±n ... -> DrawView publishOverlayState TreeFrogForceVideoRefresh isDirty` | Identical | Unchanged | Spec 14 preserved. After fixing ClearRect and overlay pipeline, preview status final will be visible without adding extra Flush (spec says only add if test demonstrates). |
| destructivePitchSample | `label Operacion Pitch... ; show 0% -> clearLogicalHistory -> captureUndo -> show15% -> build -> show55% -> malloc+memcpy if chop -> show72% -> ReplaceBuffer -> show80% -> Save -> sync -> captureRedo -> show90% -> rescale boundaries -> refreshInstrument -> prepareWaveform -> centerView -> publishOverlayState -> show100%` | Identical | Unchanged | Spec 15: progression 15,55,72,80,90,100 all produce visible frame; 100 remains active with A close etc. Preserved. |
| undo/redo | `show 10% -> capture? -> show70% -> ... -> show100%` completion overlay stays until input | Identical | Unchanged | Spec 17 |
| completion handling | `if(operationActive && percent>=100){ L1+X clear->undo; R1+X clear->redo; A clear->DrawView publishOverlayState; other consumed }` | Identical | Unchanged | Spec 16: after A, pitch/env redraws fully via DrawView; event loop's Redraw will handle Flush, but showOperationProgress already did Flush+ForceRefresh. No duplicate publishOverlayState beyond DrawView's own. Kept. |
| Flush / PostFlush ordering | `AppWindow::Flush: raster char -> TreeFrogChopperOverlayDraw -> PostFlushDraw -> GUIWindow::Flush` golden | Same before and after | Unchanged | Spec 11 ordering preserved. Manual tf_rect + ForceRefresh calls are inside showOperationProgress which is correct golden. No second parallel pipeline introduced. |
| hasWaveform gate | Golden requires hasWaveform true for pitch toggle | BEFORE removed gate (allowing pitch without playback) | AFTER kept removed (functional improvement) with safety re-validation of source buffers | Task says preserve all new functions; removing gate makes pitch visible without playback, aligning with release doc item 2. Not a visual regression. |
| safety fixes | None in golden (crash on invalid sampleIndex) | BEFORE added hasAssignedSample extra bounds + source null checks, isValidSampleIndex etc | AFTER preserved all safety (validated via grep) | Spec 13: Bacon1.4 visual semantics + current memory safety. |

## Root Cause (spec 31)

- Was ClearRect still x,y,w,h? YES (both View and AppWindow used x+w, y+h invariant). No confusion of w/h as x1/y1 in hardened version.
- Did current hardening accidentally interpret w/h as x1/y1? NO. Current View used x+w correctly. The task's hypothesized bug (x1=w, y1=h) was not present; hardening was already correct for w semantics.
- What area did ClearRect(0,8,40,14) clear BEFORE fix? Expected golden: columns 0..39 rows 8..21 = 560 cells. BEFORE with old View (x0=x, x1=x+w) cleared correctly 40x14=560. AppWindow before had no clipping, so if called via unclipped rect it would overflow, but with View's clipping it was correct. No misinterpretation.
- What area does it clear AFTER fix? Same 40x14=560, but now with full clamp and overflow safe (long long, w<=0 early). For edge case 320x240, BEFORE (View) clipped to 40x22 correctly (since x1=320->40, y1=248->30 => 40x22) but AppWindow before would have overflowed; AFTER both clip safely to 40x22.
- Did char cells repaint over tf_rect? YES, golden explicitly does ClearRect after tf_rect because Flush repaints every char cell over the pixel panel. Both BEFORE and AFTER preserve this (drawOperationOverlay: tf_rect then ClearRect). Without ClearRect, underlying text would show through.
- Was Flush itself the primary bug? NO. ShowOperationProgress always did w_.Flush + TreeFrogForceVideoRefresh. The pitch fullscreen overlay had been introduced to work around unreliable char refresh when stopped, but it introduced a second pipeline that covered status/hints and diverged from golden y=60..175. Root cause of reported "pantalla incompleta/vacia" was not Flush missing but pitch fullscreen covering entire screen and operation overlay interaction, plus AppWindow overflow risk.
- Was overlay order different from Bacon 1.4? BEFORE introduced a divergent order: pitch was drawn via direct FB fullscreen in PostFlush (TreeFrogChopperOverlayDraw) after char raster, covering everything, instead of via drawPitchScreen's char+tf_rect pipeline. AFTER restored golden order: char raster -> waveform overlay only (pitch via char), preserving status/hints.

## Build
- OLD CORE dbaed387ff72d309... 1.4M
- NEW CORE 91f2a4570747a2cc... 1.4M 2026-08-21 21:23 strip ok
- Host tests: clearrect_semantics PASS, bacon14_visual_contract PASS, chopper_draw PASS, chopper_view PASS, spectrum_analyzer PASS, host_syntax PASS, git diff --check PASS
- Visual contract: layout, rows, overlay, progress, completion, status all restored structurally.
